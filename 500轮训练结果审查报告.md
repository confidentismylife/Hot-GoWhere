# 500 轮 RL 训练结果审查报告

**审查日期**：2026-08-05

**审查对象**：云平台 500 episodes 离线 RL 训练（旧代码版本，代码重构之前运行）

**训练命令**：

```bash
python main.py --config config/mall_floorplan.yaml \
    --train-rl \
    --train-rl-episodes 500 \
    --irl-weights data/irl_weights.json \
    --rl-output data/rl_policy.json
```

**训练环境**：武汉保利广场 1F（200×120m），600 agents，6 出口，4 zones，
IRL 权重 `data/irl_weights.json`（5 人设）。

**审查产物**：`rl_policy (1).json`（用户从云平台下载的权重文件）

---

## 1. 结论（TL;DR）

1. **训练没有收敛**：PPO PolicyLoss 从第 200 轮开始爆炸，最高到 **1,203,346**（Ep 495）；
   ValueLoss 全程在 1000–1400 之间震荡，价值函数从未拟合好。
2. **日志里的“最终疏散率 38.2%”不是策略性能**：疏散率曲线（58.7% → 43.3% → 39.2% → 38.2% 冻结）
   是 `FastTrainingSimulator.reset()` 不重置体力/恐惧/目标出口造成的**状态泄漏假象**。
3. **保存的权重 ≈ 随机初始化**：与同 seed 的全新初始化逐参数对比，
   `W1` 最大偏差 ≤ 0.06、`W2` ≤ 0.02、`W3_a` ≤ 0.05，只有价值头 `W3_v` 移动了 ≤ 0.15。
   换句话说，500 轮训练几乎没把策略网络推离初始点。
4. **4 个 zone 完全同质**：各 zone 的 `W1` 两两相关系数 ≥ 0.9999，没有出现任何区域专业化。
5. **行为对比**：在同一批真实观测上，保存策略的偏好强度（\|pref\|=0.19）只比随机初始化 MLP（0.08）
   略高，且 argmax 高度集中在出口 2，与启发式的合理分流（出口 3/4）差异明显——不是“学到的东西”。
6. **结论**：该 checkpoint **不能**用于论文中任何“Ours / 训练后 RL”的结果；
   需要用修复后的代码重新训练，并按第 6 节标准验收。

---

## 2. 训练日志分析

### 2.1 关键指标随训练轮次的变化

| Ep | MeanR | PolicyLoss | ValueLoss | Evac |
|----|-------|-----------|-----------|------|
| 0   | +0.6716 | -0.0744 | 1418.26 | 58.7% |
| 5   | +0.6765 | -0.0479 | 1408.19 | 43.3% |
| 10  | +0.6756 | -0.1657 | 1369.25 | 39.2% |
| 15  | +0.6762 | +0.0127 | 1320.27 | 38.7% |
| 20  | +0.6762 | -0.0281 | 1266.23 | 38.2% |
| 50  | +0.6760 | -0.0984 | 764.64 | 38.2% |
| 100 | +0.6760 | -0.0242 | 1168.19 | 38.2% |
| 200 | +0.6760 | +0.6369 | 1137.17 | 38.2% |
| 300 | +0.6760 | +3.9506 | 1128.21 | 38.2% |
| 400 | +0.6760 | +284.87 | 1031.32 | 38.2% |
| 495 | +0.6760 | +1203346.08 | 1112.70 | 38.2% |

### 2.2 现象一：疏散率“先跌后平”是状态泄漏，不是学习曲线

- Ep 0 疏散率 58.7%：这是**启发式动作 + 满体力**下的基线（旧代码训练时
  `_pretrained_loaded=False`，rollout 全部走启发式）。
- Ep 5–15 快速下降到 38.2% 并从此冻结：
  `FastTrainingSimulator.reset()` 只重置位置/速度/存活/疏散标记，
  **不重置 stamina、fear、target_exit**。
  每轮结束后体力继续保留到下一轮，agent 越跑越慢，最终停在“全员体力耗尽”的稳态。
- 因此“Training complete. Final evacuation rate: 38.2%”反映的是模拟器缺陷，
  与策略好坏无关。

### 2.3 现象二：PPO 目标发散

- PolicyLoss 前 20 轮仍在 ±0.2 量级，随后单调失控：
  65（Ep 380）→ 464（385）→ 6592（445）→ 14857（460）→ 46668（470）→
  163836（485）→ **1203346（495）**。
- 这是典型的 PPO ratio/advantage 爆炸。旧代码用“arctanh 反推”的方式，
  把**启发式动作**当作数据动作去算当前网络下的 log-prob，行为策略与目标策略错位，
  更新目标从一开始就不自洽。

### 2.4 现象三：价值函数从未收敛

- ValueLoss 全程 1000–1400，而 MeanR 只有 0.676：价值预测与回报相差约两个数量级。
- GAE 和 advantage 都建立在不可信的价值函数上，PPO 更新的信号质量没有保证。

---

## 3. 权重文件分析

### 3.1 结构与规模

`rl_policy (1).json` 结构正确：4 个 zone，每区 `31 → 128 → 128` 骨干，
策略头 `W3_a: 128×6`（31 维观测 = 4×6 出口 + 7 标量，与 6 出口 mall 场景匹配），
价值头 `W3_v: 128×1`。所有数值有限，无 NaN。

### 3.2 保存权重 vs 全新随机初始化（同 seed 42）

| Zone | W1 max diff | W2 max diff | W3_a max diff | W3_v max diff |
|------|------------|------------|--------------|--------------|
| 0 | 0.028953 | 0.012193 | 0.024798 | 0.115177 |
| 1 | 0.059002 | 0.021147 | 0.050084 | 0.148360 |
| 2 | 0.015344 | 0.009345 | 0.005271 | 0.088097 |
| 3 | 0.049790 | 0.013683 | 0.013145 | 0.097703 |

- 骨干层 `W1/W2` 基本没动；策略头 `W3_a` 也只动了 0.05 以内；
- 唯一有明显移动的是价值头 `W3_v`（≤ 0.15），但这部分不用于部署推理。
- 结论：**500 轮训练在策略层面等效于没有训练**。

### 3.3 四区同质

4 个 zone 的 `W1` 两两相关系数均 ≥ 0.9999。初始网络同 seed、更新数据分布相近，
结果就是四个“复制粘贴”的策略，没有区域专业化。

### 3.4 行为对比（同一批真实观测，80 个 zone 观测样本）

| 策略 | \|pref\| 均值 | argmax 出口分布 |
|------|--------------|----------------|
| 启发式 | 0.390 | 出口3:37，出口4:34，出口2:3，出口5:6 |
| 保存的 500ep 策略 | 0.190 | 出口2:49，出口4:19，出口5:12 |
| 随机初始化 MLP | 0.083 | 出口2:68，出口4:12 |

保存策略只比随机 MLP“稍有一点偏好”，且偏好集中在出口 2，
与启发式的合理分流完全不同——更像是随机网络的微小扰动，而不是学到的调度知识。

### 3.5 短仿真交叉验证

在修复后的模拟器上（30 agents × 30 steps）：

| 策略 | Evac |
|------|------|
| 启发式 | 0.0% |
| 保存的 500ep 策略 | 20.0% |
| 随机初始化 MLP | 20.0% |

保存策略与随机 MLP 完全一致（短仿真样本小，仅作方向性验证；
权重层面的差异对比才是主要证据）。

---

## 4. 旧代码机制层面的根因

1. **rollout 与部署策略不一致**：训练时 `_pretrained_loaded=False`，
   `infer()` 返回启发式动作，策略网络只是事后拟合启发式轨迹的旁观者；
   保存后加载才启用 MLP——部署的是一个几乎没训练过的随机网络。
2. **reset 不完整**：stamina / fear / target_exit 跨 episode 泄漏，
   训练分布被污染，且造成疏散率曲线的“先跌后平”假象。
3. **log-prob 计算错位**：用 arctanh 反推启发式动作在当前网络下的 log-prob，
   与真正采样的行为策略不一致，PPO ratio 失去意义并最终爆炸。
4. **没有共享 critic**：旧实现是各 zone 独立价值头，且价值函数不收敛，
   advantage 噪声大。

---

## 5. 对论文/项目的影响

- 该 checkpoint 不能作为“Ours（LLM→IRL→RL）”的任何实验结果来源；
- 如果此前用该权重跑过 `rl_scheduling.enabled=true` 的验证轮次，
  其结果应标记为**无效/需重跑**；
- 论文中的疏散率数字（91.7% / 90.1% 等）本来就来自合成管线，
  与本权重无关，切勿建立任何联系；
- 仓库内另有 `data/rl_policy_test.json`（与本次审查文件不同的 checkpoint，
  同一 31 维观测结构），同样需要按第 6 节标准复核后再使用。

---

## 6. 重训与验收建议

### 6.1 重训命令（当前修复后代码）

```bash
python main.py --config config/mall_floorplan.yaml \
    --train-rl \
    --train-rl-episodes 500 \
    --irl-weights data/irl_weights.json \
    --rl-output data/rl_policy.json
```

当前代码已包含：reset 状态修复、on-policy 高斯采样、
共享集中式 critic（MAPPO/CTDE）、P-MAPPO 命名统一为 Zone-PPO。

### 6.2 训练验收标准

1. **PolicyLoss 稳定**：500 轮内应保持在 O(1) 以内，不允许出现指数级发散；
2. **疏散率曲线**：不出现“先跌后平”的模式；应与基线对比有合理改善或平台；
3. **权重 vs 初始化**：保存权重与随机初始化应有显著差异（远超 0.05 量级）；
4. **四区分化**：各 zone 权重不应两两相关 ≈ 1.0；
5. **部署行为对比**：用第 3.4 节的方法对比“训练后策略 vs 启发式 vs 随机 MLP”，
   训练后策略应比随机 MLP 明显更果断，且与启发式存在可解释差异。

### 6.3 可复用的评估方法

- 权重-初始化逐参数最大偏差表；
- 同一批真实观测上的偏好强度 / argmax 分布对比；
- 修复后模拟器上的短仿真疏散率交叉验证；
- PolicyLoss / ValueLoss / MeanR 随 episode 的曲线审查。

---

## 7. 附：本次审查涉及的关键文件

- 训练日志：用户提供的云平台控制台输出（本报告第 2 节表格来源）
- 权重文件：`C:/Users/摩天轮大盗/Downloads/rl_policy (1).json`
- 代码版本：重构前旧代码（`FastTrainingSimulator` 旧 `reset()`、
  `train_offline` 旧 rollout/log-prob 逻辑）
- 相关修复：`execution/rl_scheduler.py`、`execution/agent_factory.py`、
  `group_intel/propagation.py` 等（详见代码工作区改动）
