# 基于LLM的多角色智能体灾害人群疏散仿真系统

## —— 组会汇报完整技术文档 ——

---

# 第一部分：一分钟讲清楚这个项目

## 这个项目做了什么？

在一个电脑里建了一个**北京朝阳大悦城一楼**的完整模型，放进去 **600 个有 AI 大脑的虚拟人**，在厨房放了把火，然后观察他们怎么逃生。

**每个人脑子里都有一个大语言模型（类似 ChatGPT）在帮他思考。** 每个人能看、能想、能害怕、能累、能从众、能救人、能犯错。

这让仿真的真实性远超传统方法——传统方法里的人只是数学公式驱动的木偶。

## 一句话总结

**用 AI 造了 600 个有思想的虚拟小人，放进着火的商场里，研究怎么让人们更安全地逃生。**

---

# 第二部分：研究背景与动机

## 2.1 我们面对的现状

在全球范围内，城市商业综合体（大型购物中心、交通枢纽、体育场等）的人员密集度持续增加。以北京朝阳大悦城为例，仅一层楼的单日瞬时客流峰值就可超过 2000 人。当一个不可预知的火灾发生时，数百甚至数千人需要在有限时间和有限信息条件下做出快速撤离决策，而真实世界的教训一再表明——无序与恐慌远比火势本身造成更多伤亡。

从学术研究角度来看，大规模人群疏散行为建模面临三重矛盾：

**第一，个体决策与群体涌现的矛盾。** 商业综合体人群由高度异质的个体组成——不同年龄、不同体能、不同职业背景、不同的空间认知程度，而这些个体在灾难环境中会产生复杂的交互——信息传递、情绪感染、从众行为、合作与竞争——最终在宏观层面涌现出变化多端的人群流动模式。传统建模方式很难同时兼顾微观个体差异和宏观涌现效应。

**第二，行为真实性与计算效率的矛盾。** 要想精确刻画个体的深层认知过程需要复杂的心理和行为模型，但在数百人规模的仿真中，每增加一层计算复杂度都会对整体运行效率造成指数级影响。很多研究不得不在精细程度和运行效率之间做出取舍，一端是高度简化的"粒子驱动"模型（每个行人被视为具有简单规则的粒子），另一端是心理学启发的认知架构（如 BDI 模型、Soar、ACT-R），但这些架构部署成本极高，且难以规模化到几百甚至上千个智能体。

**第三，规则完备性与未知场景覆盖的矛盾。** 传统基于规则的智能体（Agent-Based Modeling）需要预定义所有可能的发生条件和反应策略。但在现实中，灾难场景的组合空间巨大——火源位置、出口可用性、人群密度分布、广播信息内容、个体身体状况——手工编写所有临界值（"烟雾浓度大于 0.6 就改选择其他出口"）的覆盖范围极难做到完备。况且规则编写完成后需要持续的维护更新以跟踪环境变化，这是一个高成本且容易出错的过程。

## 2.2 为什么用大语言模型？

大语言模型（LLM）在大约 2023-2024 年间展现了一个重要能力：**常识推理**。它们并非仅仅存储事实，而是表现出对日常情境的因果理解和类比推理能力。

考虑以下对比：

**传统规则系统**：
```
IF 烟雾浓度 > 0.6 THEN 远离
IF 人群密度 > 5人/㎡ THEN 减速
IF 出口距离 < 30m THEN 选择该出口
```

**LLM 决策**：
> 我观察到出口A烟雾很重（约0.72），虽然它比出口B近了将近一半路程，但烟雾正在从左向右扩散，说明出口A正在被浓烟封锁。出口B虽然远了30米但烟雾浓度很低。我还注意到有几名老人正在缓慢走向出口A，我应该去提醒他们。当前体力约55%，足够走到出口B。

区别是什么？LLM 做出了一个**多维度的、经过推理的、包含利他考量的判断**——而不仅仅是几个阈值的比较。

更关键的是，LLM 的推理过程是**可解释的**。你可以看到它为什么选择了出口B而不是出口A，这为仿真结果的可信度和可审计性提供了重要支撑。

## 2.3 核心研究问题

本项目围绕四个核心的科学问题展开：

| 问题 | 为什么重要 | 本项目的证实方式 |
|------|---------|-----------------|
| **LLM 能否生成合理、可信的疏散决策？** | 合理性是大模型用于安全关键场景的前提 | 五角色三类灾害 Prompt 体系，融合知识库 RAG，让 LLM 综合考虑距离、烟雾、体力、家庭责任 |
| **LLM 决策是否安全？有多少是危险的？** | 大模型的"幻觉"问题在安全场景中可能产生致命后果 | 七条硬约束安全护栏设计为推理时拦截层，LLM 是"建议者"，护栏是"仲裁者"，每条拦截都可审计 |
| **RTX 4090 能否支撑 600-800 个 LLM Agent？** | 消费级显卡的算力限制是所有独立研究者的共同瓶颈 | AWQ 量化 + vLLM Prefix Caching + 异步流水线 + 2% 决策抽样，单卡 50-150ms/tick |
| **QLoRA 微调能否进一步提升决策质量？** | 微调是使通用模型适应特定领域的主要手段，效果需要量化验证 | 完整微调闭环：安全护栏自动标注 → QLoRA 训练 → 基座 vs 微调 vs 规则三模型对比 |

---

# 第三部分：系统架构全景

## 3.1 三层认知架构

整个系统按照"感知 → 认知 → 行为"的心理学模型组织为三个层次：

```
┌──────────────────────────────────────────────────────────────────┐
│                                                                   │
│   ①  感  知  层  (Perception)                                     │
│                                                                   │
│   ┌──────────────┐   ┌──────────────┐   ┌──────────────┐        │
│   │  环境仿真器    │   │  VLM 视觉感知 │   │  YOLO 检测   │        │
│   │              │   │              │   │              │        │
│   │ 元胞自动机    │   │ Qwen-VL-7B   │   │ YOLOv8n      │        │
│   │ 火/烟/热扩散  │   │ 画面→中文描述 │   │ 人数+密度    │        │
│   │ 结构完整性    │   │              │   │ 异常事件识别  │        │
│   └──────┬───────┘   └──────┬───────┘   └──────┬───────┘        │
│          │                  │                  │                  │
│          └──────────────────┼──────────────────┘                  │
│                             │                                     │
│               ┌─────────────┴─────────────┐                      │
│               │  自然语言转换器 (NL Converter) │                   │
│               │  多模态数据 → 结构化中文文本     │                   │
│               └─────────────┬─────────────┘                      │
│                             │                                     │
├─────────────────────────────┼─────────────────────────────────────┤
│                             ▼                                     │
│   ②  决  策  层  (Decision)                                      │
│                                                                   │
│   ┌──────────────┐   ┌──────────────┐   ┌──────────────┐        │
│   │  Prompt 管理器 │   │  vLLM 推理引擎│   │  安全护栏     │        │
│   │              │   │              │   │              │        │
│   │ 5角色×3灾害   │   │ AWQ 4-bit量化 │   │ 7条硬约束     │        │
│   │ 15套模板     │   │ 连续批处理    │   │ LLM→拦截→修正  │        │
│   │ JSON强制输出  │   │ 异步流水线    │   │ 可审计日志    │        │
│   └──────┬───────┘   └──────┬───────┘   └──────┬───────┘        │
│          │                  │                  │                  │
│          │         ┌────────┴────────┐         │                  │
│          │         │   知识库 RAG    │         │                  │
│          │         │  22条消防规则   │         │                  │
│          │         │  ChromaDB语义检 │         │                  │
│          │         │  分层注入: 市民  │         │                  │
│          │         │  →常识 专家→专业 │         │                  │
│          │         └────────┬────────┘         │                  │
│          └──────────────────┼──────────────────┘                  │
│                             │                                     │
├─────────────────────────────┼─────────────────────────────────────┤
│                             ▼                                     │
│   ③  执  行  层  (Execution)                                     │
│                                                                   │
│   ┌──────────────┐   ┌──────────────┐   ┌──────────────┐        │
│   │  总调度器     │   │  群体信息传播 │   │  物理引擎     │        │
│   │              │   │              │   │              │        │
│   │ 每tick循环    │   │ 口口相传     │   │ 社会力模型    │        │
│   │ 感知→决策→执行│   │ 恐惧扩散     │   │ Numba JIT加速 │        │
│   │ 统计+渲染    │   │ 体力消耗     │   │ 5力并行计算   │        │
│   └──────────────┘   └──────────────┘   └──────────────┘        │
│                                                                   │
└──────────────────────────────────────────────────────────────────┘
```

## 3.2 数据流全景

每一 tick（0.1 秒）的完整数据流如下：

```
                         开始 Tick N
                              │
                  ┌───────────┴───────────┐
                  │   1. 环境更新           │
                  │   DisasterSimulator.    │
                  │   step(dt=0.1)          │
                  │   → 火扩散 + 烟扩散      │
                  └───────────┬───────────┘
                              │
                  ┌───────────┴───────────┐
                  │   2. 环境快照           │
                  │   DisasterSimulator.    │
                  │   snapshot()            │
                  │   → EnvironmentSnapshot │
                  └───────────┬───────────┘
                              │
                  ┌───────────┴───────────┐
                  │   3. 选出决策者          │
                  │   约 2%/tick 需重决策     │
                  │   → 12-20 agents/tick   │
                  └───────────┬───────────┘
                              │
                  ┌───────────┴───────────┐
                  │   4. NL转译+KB检索      │
                  │   NLConverter.          │
                  │   full_context()         │
                  │   + KnowledgeBase.       │
                  │   query()                │
                  │   → 中文Prompt           │
                  └───────────┬───────────┘
                              │
                  ┌───────────┴───────────┐
                  │   5. vLLM批量推理       │
                  │   submit_batch()        │
                  │   → 异步后台线程         │
                  └───────────┬───────────┘
                              │
                  ┌───────────┴───────────┐
                  │   6. 收集结果+安全审查   │
                  │   collect_results()     │
                  │   + SafetyGuard.check() │
                  │   → 最终决策            │
                  └───────────┬───────────┘
                              │
                  ┌───────────┴───────────┐
                  │   7. 群体信息传播        │
                  │   GroupIntel.propagate()│
                  │   → 口口相传+恐惧扩散    │
                  └───────────┬───────────┘
                              │
                  ┌───────────┴───────────┐
                  │   8. 物理步进           │
                  │   BatchedPhysics.       │
                  │   step_all()            │
                  │   → 600人×5力, 3ms      │
                  └───────────┬───────────┘
                              │
                  ┌───────────┴───────────┐
                  │   9. 统计+渲染          │
                  │   更新逃出/死亡/活跃     │
                  │   每10tick渲染一帧       │
                  └───────────┬───────────┘
                              │
                            Tick N+1
```

---

# 第四部分：感知层 —— Agent 如何感知世界

## 4.1 环境仿真器（`perception/environment.py`）

### 元胞自动机火灾模型

环境是一个 **150m × 80m** 的连续空间，离散化为 **300 × 160** 的网格，每个网格的大小为 0.5m × 0.5m。

每个网格有 **4 个通道**：

| 通道 | 含义 | 值范围 | 更新方式 |
|------|------|--------|---------|
| 通道 0 | 烟雾浓度 | 0.0（无烟）~ 1.0（完全不透明） | 扩散方程 + 火源持续产生 |
| 通道 1 | 温度 | 20°C（室温）~ 800°C+（火心） | 热传导 + 火源加热 |
| 通道 2 | 结构完整性 | 1.0（完好）~ 0.0（坍塌） | 高温持续暴露造成衰减 |
| 通道 3 | 明火状态 | 0.0（无火）~ 1.0（充分燃烧） | 概率点火 + 持续燃烧 |

### 火灾扩散算法

每个 tick（0.1 秒），对每个网格单元执行：

```
对于每个未着火的格子 (r, c)：
  if wall_mask[r][c] == True:
    skip  # 墙壁不会着火

  neighbor_fire = 统计周围 8 个格子的火势值之和

  if neighbor_fire > 0:
    spread_prob = min(1.0, neighbor_fire * spread_rate * dt)
    if random() < spread_prob:
      grid[r][c][FIRE] = 0.3          # 初燃
      grid[r][c][TEMP] = 200          # 升温

对于已经着火的格子：
  grid[r][c][FIRE] = min(1.0, fire + 0.5 * dt)  # 火势增长
  grid[r][c][TEMP] = min(800, temp + 100 * dt)   # 温度攀升
  grid[r][c][SMOKE] = min(1.0, smoke + 0.8 * dt) # 产生烟雾
```

### 烟雾扩散算法

```
smoke_next = smoke.copy()

对于每个格子 (r, c)：
  laplacian = (邻居烟雾平均值 - 当前烟雾值)
  smoke_next[r][c] += diffusivity * laplacian * dt

  加上火源的贡献：
  if grid[r][c][FIRE] > 0:
    smoke_next[r][c] += fire * smoke_production_rate * dt

最后：
  grid[:, :, SMOKE] = clip(smoke_next, 0, 1)
  grid[wall_mask][SMOKE] = 0  # 墙壁不透烟
```

### 随时间变化的视觉效果

```
初始：          30秒后：             60秒后：             120秒后：
░░░░░░░░        ░░      ░░          ░░░░░░▓▓            ░░░░▓▓▓▓▓▓
░░░░🔥░░        ░░🔥🔥  ░░          ░░░░▓▓▓▓▓            ░░░▓▓▓▓▓▓▓
░░░░░░░░        ░░🔥🔥🔥░░          ░░░▓▓▓▓▓▓▓            ░░▓▓▓▓▓▓▓▓
░░░░░░░░        ░░  🔥  ░░          ░░░░░▓▓▓▓▓            ░░░▓▓▓▓▓▓▓
░░░░░░░░        ░░      ░░          ░░    ░░░░▓            ░░░░░▓▓▓▓

(░ = 轻烟, ▓ = 浓烟+明火)
```

### 墙壁遮罩（Wall Mask）

火焰和烟雾无法穿透墙壁。系统使用 floorplan 提供的 wall_mask：

```python
# 在环境更新循环中：
self.grid[self.wall_mask, 0] = 0.0  # 墙壁格子的烟雾始终为 0
self.grid[self.wall_mask, 3] = 0.0  # 墙壁格子的火势始终为 0
```

### EnvironmentSnapshot —— 环境快照

每个 tick 创建一份快照，供 Agent "读取"：

```python
@dataclass
class EnvironmentSnapshot:
    tick: int                    # 当前 tick 编号
    timestamp: float             # 仿真时间（秒）
    width: float                 # 场景宽度
    height: float                # 场景高度
    grid: np.ndarray             # 4 通道网格（完整副本）
    grid_resolution: float      # 每个格子的实际大小
    exits: List[Tuple]          # 出口位置列表
    obstacles: List[dict]       # 障碍物列表
    official_broadcast: str     # 官方广播内容
    disaster_type: str          # 灾害类型（fire/earthquake/flood）

    def smoke_at(self, pos):    # 查询某位置的烟雾浓度
    def temperature_at(self, pos):  # 查询某位置的温度
    def is_on_fire(self, pos):      # 判断某位置是否有明火
    def structural_at(self, pos):   # 查询某位置的结构完整性
```

---

## 4.2 自然语言转换器（`perception/nl_converter.py`）

这是系统中一个关键但容易被忽视的模块。它的作用是把数据世界翻译为人类语言。

### 为什么需要 NL 转换？

大语言模型是一个**文本到文本**的系统。它接收自然语言提示词（Prompt）作为输入，并生成自然语言文本作为输出。环境数据（数字矩阵、坐标、浓度浮点数）对 LLM 来说是无意义的。用自然语言描述当前场景是连接环境与 LLM 的必经桥梁。

### NL Converter 的六个职责

NL Converter 负责生成六个信息块，按固定顺序拼接为每条 LLM Prompt 的 User Message：

**1. 环境上下文（`environment_context`）**
```
[环境状态]
时间: 45秒
位置: (52.3, 30.1)
烟雾: 轻微烟雾 (浓度10%)
温度: 正常 (25°C)
建筑结构: 完好

出口状态:
  出口1(25,0): 距离28.5m, 状态: 通畅
  出口2(75,0): 距离23.1m, 状态: 浓烟封锁
  出口3(148,40): 距离95.7m, 状态: 轻微烟雾
  出口4(150,65): 距离110.2m, 状态: 火灾危险

官方广播: 全体注意：东区走廊发生火灾，请从西侧出口撤离
```

核心转换逻辑——将连续数值映射为人类的定性描述：

| 数据范围 | 翻译结果 |
|---------|---------|
| smoke < 0.1 | "几乎无烟" |
| smoke 0.1~0.3 | "轻微烟雾" |
| smoke 0.3~0.6 | "烟雾较浓" |
| smoke > 0.6 | "浓烟弥漫" |
| temp < 35 | "正常" |
| temp 35~60 | "明显升高" |
| temp 60~150 | "灼热" |
| temp > 150 | "极高，有生命危险" |
| exit_smoke < 0.3 | "通畅" |
| exit_smoke 0.3~0.6 | "有烟雾" |
| exit_smoke > 0.6 | "浓烟封锁" |

**2. 个人信息上下文（`personal_context`）**
```
[个人状态]
agent_3a2f7b: 72岁普通市民
环境熟悉度: 不熟悉
体力: 一般 (55/100)
心理状态: 非常恐惧
当前行动: walk
```

**3. 记忆上下文（`memory_context`）**
```
[最近记忆]
  - [38.0s] 出口2被浓烟封锁，无法通过
  - [25.0s] 看到消防员正向出口3方向移动
  - [10.0s] 听到广播说东区发生火灾
```

每位 Agent 在当前仿真过程中经历过的重要事件被保留在记忆中，在下一次 LLM 决策时提供情境化的历史信息。

**4. 流言上下文（`rumor_context`）**
```
[最近消息] 有人说出口4已经完全被火堵死
```

流言是群体智能中的"口口相传"机制——Agent 从周围人那里获得信息，但这些信息未必完全准确。

**5. VLM 上下文（`vlm_context`）** — 可选，仅在开启 VLM 时启用
```
[监控画面分析]
画面中可见约40-50人，主要分布在走廊区域。
左侧走廊烟雾能见度约15米。
三个出口指示牌仍然可见。
```

**6. YOLO 上下文（`yolo_context`）** — 可选，仅在开启 YOLO 时启用
```
[人群检测] 画面中检测到约47人
  人群热点: (50,30)附近约23人聚集; (60,45)附近约15人聚集
  ⚠ 出口2区域出现拥堵
```

六个信息块按以下优先级整合：

```
full_context = environment_context + vlm_context + yolo_context
             + personal_context + memory_context + rumor_context
```

### NL Converter 的关键设计特点

**第一，通道感知的排序**。环境信息排在个人状态之前，这是有意为之——LLM 倾向于赋予早期 token 更高注意力权重，将环境（客观事实）放在个人（主观状态）之前有助于 Agent 首先关注客观条件。

**第二，Agent 差异化**。每个 Agent 的 Prompt 都是单独生成的——同一个出口的距离信息随 Agent 当前位置变化，烟雾定性描述随 Agent 距离自适应调整。这种细粒度的个体差异化是使得 600 个 LLM Agent 表现出"多元化行为"的关键。

**第三，信息限流**。不把全部数据都塞进 Prompt——只选最近 3 条记忆事件、最近 1 条流言、3 个密度热点。这是 Prompt 长度的刻意约束，也是认知合理性的体现（人类也不记得过去发生的所有事）。

---

## 4.3 商场平面图（`perception/floorplan.py`）

### 真实场景建模

选用**北京朝阳大悦城 1F** 的真实楼层布局进行建模。

**朝阳大悦城 1F 的基本规模**：
- 建筑尺寸：150m × 80m
- 中间有一个大型中庭（约 30m × 20m）
- 环形走廊围绕中庭，将四周商铺串联
- 30+ 个商铺，包括：ZARA、H&M、优衣库、海底捞、Apple Store、华为体验店、星巴克、餐饮区等
- 8 个疏散出口分布在四面外墙

### 网格化表示

将整个一层平面离散化为网格：

```
网格分辨率 = 0.5m/格
总列数 = 150 / 0.5 = 300
总行数 = 80 / 0.5 = 160
总格子 = 300 × 160 = 48,000

每个格子的值：
  0 = 可通行（空地、走廊、开放区域）
  1 = 不可通行（墙壁、柱子、商铺隔断）
  2 = 出口区域
```

### 商铺和走廊的布局示意

```
                    ← 150 米 →
    ┌──────────────────────────────────────────────┐
    │  █E1         █E2          █E3         █E4   │
    │  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐     │
    │  │ZARA  │  │ H&M  │  │优衣库│  │海底捞│     │
    │  └──┬───┘  └──┬───┘  └──┬───┘  └──┬───┘     │
    │     │         │         │         │          │
    │  ═══╧═════════╧════ 中庭 ╧═════════╧═══      │  ↑
    │  ┌──┴───┐  ┌──────┐  ┌──────┐  ┌──┴───┐     │  80m
    │  │星巴克│  │Apple │  │ 华为 │  │餐饮区│ 🔥  │     │
    │  └──────┘  └──────┘  └──────┘  └──────┘     │  ↓
    │  █E5         █E6          █E7         █E8   │
    └──────────────────────────────────────────────┘

    █ = 出口 (Exit)
    ═ = 环形走廊
    ┌──┐ = 商铺墙壁
    🔥 = 火灾起源 (厨房/餐饮区)
```

### 出口体系

8 个出口分布在商场四面，每个出口的位置设在外墙开口处：

| 出口 | 位置 | 面朝 | 附近商铺 |
|------|------|------|---------|
| E1 | (12, 2) | 北墙西段 | ZARA |
| E2 | (45, 2) | 北墙中段 | H&M |
| E3 | (75, 2) | 北墙中段 | 优衣库 |
| E4 | (138, 2) | 北墙东段 | 海底捞 |
| E5 | (12, 78) | 南墙西段 | 星巴克 |
| E6 | (45, 78) | 南墙中段 | Apple |
| E7 | (90, 78) | 南墙中段 | 华为 |
| E8 | (138, 78) | 南墙东段 | 餐饮区（火源附近）|

### 墙壁网格在物理引擎中的使用

墙壁网格在物理引擎的 JIT 内核中作为**排斥力来源**：

```python
# 在 Numba JIT 内核中（basically C speed）
for i in range(N):  # 对每个 Agent
    # 找出 Agent 周围 2m 范围内的墙壁格子
    for wr in range(r_min, r_max):
        for wc in range(c_min, c_max):
            if wall_grid[wr * wg_cols + wc] == 1:
                # 墙壁格子 → Agent 的压力
                dist = distance(agent_pos, wall_cell_center)
                if dist < 1.5:  # 1.5 米内开始排斥
                    force = 15000 * (1.5 - dist)  # 弹簧刚度 15000 N/m
                    # 方向：墙壁格子 → Agent（即排斥力）
                    fx += force * (agent_x - wall_x) / dist
                    fy += force * (agent_y - wall_y) / dist
```

这个计算确保 Agent 不会穿越墙壁，同时在靠近墙壁时会被推开。

---

## 4.4 高级感知（VLM + YOLO —— 可选模块）

这两个模块默认关闭（每个需要额外 5GB 显存）。

### VLM 视觉语言模型（`perception/vlm_perceiver.py`）

使用 **Qwen2.5-VL-7B-Instruct-AWQ**（70 亿参数，4-bit 量化）：

```
输入：商场俯视图渲染截图（PIL Image）
    ↓
Qwen-VL-7B 多模态推理
    ↓
输出：中文场景描述
"当前监控可见约40-50人，主要集中在走廊中部。
 左侧走廊被浓烟覆盖，能见度约15米。
 出口E3、E4的指示牌还能看见。
 没有发现明显的人员拥挤或踩踏风险。"
```

架构相对简单——将渲染帧送入视觉语言模型，生成一份自然语言描述，再像其他 Prompt 一样注入 LLM 的决策上下文。相当于给 Agent 加装了一台 CCTV 监视器。

### YOLO 行人检测（`perception/yolo_detector.py`）

使用 **YOLOv8n**（最小版本，约 0.04GB 显存）：

```
输入：商场俯视图渲染截图
    ↓
YOLOv8n 目标检测
    ↓
输出：结构化检测报告
{
    person_count: 47,
    density_hotspots: [
        {center: (50, 30), count: 23},
        {center: (60, 45), count: 15},
    ],
    abnormal_events: ["出口2区域出现拥堵"],
}
```

YOLO 提供定量信息（"有多少人"、"人都在哪里"），与 VLM 提供的定性描述（"环境看起来怎样"）形成互补。

---

# 第五部分：决策层 —— Agent 如何思考和做决定（核心）

决策层是整个系统的大脑中枢。它接收感知层生成的"当前情况描述"，输出每个 Agent 的行为决策。这是整个项目最核心的部分。

## 5.1 Agent 的静态属性（`decision/agent_state.py`）

每个 Agent 是结合了静态个体特征和动态状态变化的数据结构。

### AgentProfile（静态/不会变）

```python
@dataclass
class AgentProfile:
    id: str                       # 唯一标识
    age: int                      # 5-80
    role: str                     # civilian | firefighter | ...
    occupation: str               # 职业描述
    max_speed: float              # 最快能跑多快 (0.8-2.0 m/s)
    familiarity: float            # 对商场的熟悉度 (0-1)
    risk_aversion: float          # 风险回避程度 (0-1,越高越保守)
    altruism: float               # 利他主义程度 (0-1,越高越愿意帮别人)
    equipment: str                # 携带装备 (消防员有呼吸器等)
    family_group_id: Optional[str] # 家庭归属 (如与家人同来)
```

**年龄分布**（从 mall_floorplan.yaml 配置中读取）：
| 年龄段 | 占比 | 速度范围 |
|--------|------|---------|
| 年轻 (18-35) | 40% | 1.5-2.0 m/s |
| 中年 (36-55) | 40% | 1.2-1.6 m/s |
| 老年 (56-80) | 15% | 0.8-1.2 m/s |
| 儿童 (5-17) | 5% | 1.0-1.5 m/s |

**熟悉度分布**：
| 级别 | 占比 | 含义 |
|------|------|------|
| 低熟悉 (0-0.3) | 40% | 第一次来商场，不认识路 |
| 中熟悉 (0.3-0.7) | 40% | 来过几次，大致知道出口位置 |
| 高熟悉 (0.7-1.0) | 20% | 店员和常客，知道所有出口甚至后门 |

### AgentDynamic（随时间变化）

```python
@dataclass
class AgentDynamic:
    # 物理状态
    position: np.ndarray          # (x, y)
    velocity: np.ndarray          # (vx, vy)
    alive: bool                   # 是否还活着
    evacuated: bool               # 是否已逃出
    injured: bool                 # 是否受伤

    # 生理状态
    stamina: float                # 剩余体力 (0-100)
    fear_level: float             # 恐惧度 (0-10)

    # 决策状态
    speed_choice: Speed           # 当前的速度选择 (run/walk/crawl/wait)
    target_exit: Optional[np.ndarray]  # 目标出口坐标
    target_exit_idx: int          # 目标出口编号
    cooperation_mode: Cooperation # 协作模式 (none/help_family/...)
    reasoning_text: str           # 上一次 LLM 决策的推理文本
    last_decision_tick: int       # 上次做决策的 tick
    has_new_info: bool            # 是否有新信息（触发重决策）

    # 信息状态
    memory_events: List[dict]     # 记忆中的关键事件
    received_rumors: List[dict]   # 从他人那听到的流言
    known_exits: List[float]      # 已知出口的烟雾/火势信息

    # 家庭关系
    family_member_ids: List[str]  # 家人的 agent ID
    is_with_family: bool          # 是否与家人在一起
```

## 5.2 角色体系（`decision/agent_roles.py`）

五种角色定义如下：

| 角色 | 占比 | 核心职责 | 行为特点 |
|------|------|---------|---------|
| 🧑 **civilian** | ~92% | 保护自己和家人，找到出口 | 自私/利他混合，恐慌程度各异，随机应变 |
| 🎖 **global_commander** | 1人 | 评估全局态势，发布广播指令 | 全局视角，决策影响所有 Agent |
| 📡 **area_commander** | 2-3人 | 负责区域疏散协调 | 半全局视角，负责分区调度 |
| 🚒 **firefighter** | ~3% | 搜救被困人员，控制火势 | 高风险容忍度，趋向火源和被困人员 |
| 🧭 **guide** | ~3% | 带领人群向出口有序疏散 | 高商场熟悉度，主动招揽跟随者 |

每种角色有自己的决策输出结构：

```python
# 普通市民决策
@dataclass
class DecisionResult:
    agent_id: str
    target_exit_idx: int
    target_exit_pos: Tuple[float, float]
    speed: Speed              # run | walk | crawl | wait
    cooperation: Cooperation  # none | help_family | follow_crowd | lead_others
    reasoning: str            # 综合推理过程 (2-3句话)
    risk_assessment: str      # 风险评估 (1句话)
    compute_time: float       # LLM推理耗时

# 指挥官决策
@dataclass
class CommanderDecision:
    agent_id: str
    situation_assessment: str         # 态势评估
    area_priorities: List[dict]       # 区域优先级
    broadcast_message: str            # 广播内容 (简洁有力)
    resource_allocations: List[dict]  # 资源调度
    compute_time: float

# 消防员决策
@dataclass
class FirefighterDecision:
    agent_id: str
    action: str              # move | suppress_fire | rescue | report
    target_position: Tuple[float, float]
    rescue_target_ids: List[str]
    fire_suppression_point: Tuple[float, float]
    speed: str
    reasoning: str
    compute_time: float

# 引导员决策
@dataclass
class GuideDecision:
    agent_id: str
    target_exit_idx: int
    route_description: str
    speed: str
    call_for_followers: bool  # 是否主动招揽跟随者
    reasoning: str
    compute_time: float
```

## 5.3 Prompt 管理系统（`decision/prompt_manager.py`）

### 为什么 Prompt 管理如此重要？

不同角色需要不同风格的提示词。一个消防员的角色设定和决策框架与一个普通市民完全不同。写作方式类似但又不同。如果所有角色共享同一套 prompt 框架，它们的行为会过于趋同——而真实世界的疏散场景中个体间的多样化反应才是有效性的关键。

### 系统 Prompt = System Message（告诉 AI 它是谁）

**普通市民的 System Prompt**：
```
你是一个在火灾中进行疏散的普通人。
你需要根据环境信息做出理性的疏散决策。

你的决策原则:
1. 安全优先: 选择风险最低的出口和路线
2. 量力而行: 体力不足时不要奔跑,受伤后降低速度
3. 信息判断: 官方广播通常可靠,但也要观察实际环境
4. 家庭责任: 如果有家人在附近,应该互相照应
5. 适应性: 如果原定路线出现危险,及时调整计划

[在此插入 RAG 检索到的逃生知识——"你记得的安全常识"]

你必须以JSON格式返回决策。只返回JSON,不要添加任何其他文字。
```

**消防员的 System Prompt**：
```
你是一名配备{装备}的消防员,正在火灾灾害现场执行任务。

你的职责:
1. 搜救被困人员(体力耗尽、受伤、被困在烟雾中的平民)
2. 控制火势蔓延(在保证自身安全的前提下)
3. 向指挥中心报告火势发展和危险区域

行动原则:
- 自身安全第一: 不进入结构即将坍塌的区域,烟雾>80%时佩戴呼吸器
- 优先救最危险的人: 位于火源附近、体力耗尽、儿童和老人
- 与队友保持联系: 用对讲机报告位置和发现
- 灭火策略: 先控制蔓延方向,再扑灭火源

[在此插入 RAG 检索到的专业知识——"消防员专业逃生知识"]

你必须以JSON格式返回决策。只返回JSON,不要添加任何其他文字。
```

### User Prompt = User Message（告诉 AI 当前情况）

这是 NL Converter 合成的个性化描述 + JSON 输出格式要求。

普通市民的 User Prompt 框架：
```
[NL Converter 合成的个性化场景描述]

请评估当前情况并做出疏散决策。按以下JSON格式返回:

{
  "risk_assessment": "当前最大风险(1句话)",
  "target_exit": "出口编号(如: 出口1)",
  "route_reasoning": "选这条路线的理由(1句话)",
  "speed": "run|walk|crawl|wait",
  "cooperation": "none|help_family|follow_crowd|lead_others",
  "reasoning": "综合决策理由(2-3句话)"
}
```

### Prompt 设计的三条原则

1. **JSON 输出结构化**——要求 LLM 只返回 JSON，不返回解释。这保证了输出可以被程序自动解析。（在部署时系统调用 JSON 解析器自动提取字段。）

2. **思维链推理（Chain-of-Thought）**——`reasoning` 字段是一个思维链机制，要求 LLM 在输出最终决策前说明推理过程。这既提供了可解释性（可以审计每个决策），也通过思维链效应减少了错误决策。

3. **角色适配**——不同的 System Prompt 和输出 schema 意味着不同角色在 LLM 内的 "角色设定"完全不同。消防员的输出包含 `action`（行动类型）和 `fire_suppression_point`（灭火坐标），而普通市民的输出包含 `target_exit`（目标出口）和 `cooperation`（协作模式）。每个角色只包含对自己有意义的任务。

### JSON 解析的鲁棒性

LLM 并不总是完美返回 JSON。在实际部署时会遇到各种格式错误：

```
❌ 错误格式 1: 被 Markdown 包裹
```json
{...}
```

❌ 错误格式 2: JSON 前后有多余文字
"这是我的决策：{...}谢谢你。"

❌ 错误格式 3: 截断的 JSON
{"target_exit": "出口1", "reasoning": "因
```

Robust JSON 解析器使用了多种策略：检测并去除 Markdown 代码块、从第一个 `{` 到最后一个 `}` 提取、尝试用 `json.loads()` 标准解析、如果失败则尝试修复常见的语法错误。

## 5.4 vLLM 推理引擎（`decision/cognitive_engine.py`）

### 核心挑战

RTX 4090 有 24GB 显存。但如果直接运行全精度的 Qwen2.5-3B 模型，加上 600 个 Agent 的 KV Cache，总显存需求会轻松超过 24GB。而且如果每个 tick 都对所有 600 个 Agent 做 LLM 推理，即使最快的 GPU 也需要数秒——远超 0.1 秒的 tick 间隔。

### 四大优化策略

**(1) AWQ 4-bit 量化**

采用 AWQ（Activation-aware Weight Quantization）将模型从 FP16（半精度，约 6GB）压缩到 4-bit 整数（约 2GB）。AWQ 通过分析每一层权重对激活值的影响来自动选择哪些通道该分配更多位数——效果与 FP16 几乎无差异。

**结果**：模型显存减少三分之二，剩余 22GB 用于 KV Cache。

**(2) Prefix Caching**

vLLM 的 `enable_prefix_caching=True` 利用了多个请求共享相同 System Prompt 前缀的事实。

```
普通 LLM 推理（无缓存）：
  请求 1: [System Prompt (400 tokens)] [User Prompt 1 (200 tokens)] → 600 tokens 编码
  请求 2: [System Prompt (400 tokens)] [User Prompt 2 (200 tokens)] → 600 tokens 编码
  请求 3: [System Prompt (400 tokens)] [User Prompt 3 (200 tokens)] → 600 tokens 编码
  共: 400 × 3 + 200 × 3 = 1800 tokens 编码

Prefix Caching：
  请求 1: [System Prompt (400 tokens)] [User Prompt 1 (200 tokens)] → 全部 600 tokens 编码 + 缓存
  请求 2: [System Prompt ✓ 复用]    [User Prompt 2 (200 tokens)] → 200 tokens 编码
  请求 3: [System Prompt ✓ 复用]    [User Prompt 3 (200 tokens)] → 200 tokens 编码
  共: 400 + 200 × 3 = 1000 tokens 编码（节省 44%）
```

相同的角色 + 相同的灾害类型的 System Prompt 完全相同，而 User Prompt 随 Agent 不同而变化。vLLM 的 prefix caching 自动识别共享前缀块。

**(3) 异步推理流水线**

这在设计上是最大的性能飞跃。LLM 推理不是同步的——Agent 不需要等待 LLM 回复才能继续移动。

```
时间轴（同步 vs 异步）：

同步（主线）：
  tick N: [物理 3ms] → [等待LLM 200-800ms] → [应用决策] → tick N+1
  问题：每个 tick 都要卡 200-800ms 等 LLM

异步（本项目）：
  tick N:   [物理 3ms] → [送出 LLM 请求，不等待] → tick N+1
  tick N+1: [物理 3ms] → [LLM 请求还在处理中] → tick N+2
  tick N+2: [物理 3ms] → [LLM 结果回来了！] → [应用新决策] → tick N+3

  关键：物理引擎和 LLM 推理并行运行，不互相阻塞。
```

实现细节：
```python
def submit_batch(self, agents, env, knowledge_docs_map):
    """非阻塞——只是放缓冲区然后返回"""
    with self._inference_lock:
        for a in agents:
            self._pending_agents[a.id] = (a, env)

def collect_results(self):
    """如果推理线程完成了，取回结果"""
    if self._inference_done:
        results = self._results
        self._inference_done = False
        return results
    return {}  # 还没完成，返回空
```

**(4) 决策采样（Decision Sampling）**

不是每个 Agent 每 tick 都重新思考。Agent 每 30 tick（3 秒）或者有重大信息变化时才重新调用 LLM：

```python
# 选出需要重决策的 Agent
agents_to_decide = [
    a for a in self.agents
    if (a.dynamic.alive and not a.dynamic.evacuated and
        (a.dynamic.has_new_info or
         self.tick - a.dynamic.last_decision_tick >= self.decision_ticks))
]
```

这导致每 tick 只有约 2% 的 Agent 需要重新决策（约 12-20 人）。3 秒的间隔对人群疏散场景来说是合理的——人类在逃生过程中不会每秒都在重新评估，而是维持已有的目标方向直到观察到显著变化。

## 5.5 RAG 知识库（`decision/knowledge_base.py`）

### 为什么需要知识库？

基于 Qwen2.5-3B 的通用模型基于 14.8T tokens 的公开网页数据进行训练。火灾安全知识（疏散策略、消防员战术程序、火灾物理特性）在预训练中占比极小。让 LLM "凭空"做出专业判断相当于让一个没受过培训的人做出消防决策。

RAG（检索增强生成）在做出每个决策时，先从知识库检索相关的专业知识，作为 System Prompt 的一部分提供给 LLM。这相当于 LLM 在考试时能"查笔记"。

### 知识库内容

预编译的 22 条中文安全规则，按灾害类型组织：

| 类别 | 条数 | 示例 |
|------|------|------|
| **火灾** | 8 | "烟向上聚集，地面空气相对清洁，弯腰低姿前进"、"勿乘电梯，电梯井会形成烟囱效应"、"门把手发烫说明门外有火"、"身上着火时应停下-倒下-滚动" |
| **地震** | 5 | "趴下-掩护-抓牢"、"远离建筑物和电线杆"、"地下空间注意头顶坠落物" |
| **洪水** | 3 | "15cm流水即可把人冲倒"、"地下车库进水时立即弃车" |
| **通用** | 6 | "保持冷静最重要"、"提前观察疏散图"、"帮助弱势群体" |

### 分层知识注入

**这是本项目的一个关键设计选择**——不同角色获得不同深度的知识。

```
普通市民的查询:
  搜索 "通用安全常识" → disaster_type="general"
  结果: "保持冷静，听从指挥"、"提前观察疏散路线"

消防员/指挥/引导员的查询:
  搜索 "火灾疏散决策" → disaster_type="fire"
  结果: "烟向上聚集，地面空气较清洁"、"优先选择防火楼梯间"
```

实现了知识获取的社会分层——与真实世界一样，专业人员经过培训掌握了普通人没有的逃生知识。

### 检索方式

**主方案：ChromaDB 语义搜索**
```
User Query: "火灾疏散决策"
    ↓
BGE-small-zh-v1.5 Embedding
    ↓
ChromaDB cosine similarity search
    ↓
Top-3 最相关文档
```

**降级方案：Bigram 关键词评分**

当 ChromaDB 不可用时（兼容性问题），使用基于 bigram（双字）重叠的评分作为轻量级降级方案：

```python
def _keyword_search(self, query, disaster_type, top_k):
    query_bigrams = {query[i:i+2] for i in range(len(query)-1)}

    candidates = []
    for dtype in [disaster_type, "general"]:
        for idx in self._doc_index.get(dtype, []):
            candidates.append(self._docs[idx])

    scored = []
    for doc in candidates:
        doc_bigrams = {doc[i:i+2] for i in range(len(doc)-1)}
        score = len(query_bigrams & doc_bigrams) / max(len(query_bigrams), 1)
        scored.append((score, doc))

    scored.sort(key=lambda x: x[0], reverse=True)
    return [doc for _, doc in scored[:top_k]]
```

### 缓存策略

知识库内容不会在仿真过程中改变——逃生原则是不随时间而变的。因此查询结果被缓存 30 tick（3 秒），期间所有 Agent 共享同一份检索结果。

## 5.6 安全护栏（`decision/safety_guard.py`）——论文的核心贡献

### 设计哲学

安全护栏是 LLM 的"安全检查员"。不同于 RLHF（强化学习人类反馈，在训练阶段修正模型行为），本项目的安全护栏是**推理时拦截**——LLM 输出后、应用到 Agent 之前，被逐条审查。

这样做的优势：
- **实时可调**：调整阈值不需要重新训练模型
- **完全可审计**：每条拦截都有日志，可以事后追溯
- **零误判透明**：规则显式定义，不存在"模型自己判断"的模糊地带

### 七条硬约束

| # | 检查项 | 触发条件 | 行动 |
|---|--------|---------|------|
| 1 | **出口烟雾封锁** | Agent 选择的出口烟雾浓度 > 0.6 | 拒绝该出口，改用烟雾最低且安全的出口 |
| 2 | **路径经过火源** | Agent 到目标出口的路径上有火 | 改为选择绕过火源的安全出口 |
| 3 | **体力-速度匹配** | stamina < 20 但 agent 选择 run | 降级为 walk |
| 4 | **极低体力** | stamina < 10 但 agent 选择 run/walk | 强制降级为 crawl |
| 5 | **受伤限制** | injured=True 但 speed=run | 强制降级为 walk |
| 6 | **距离合理性** | 目标出口距离 > 150m | 拒绝该选择，切换到最近的合理出口 |
| 7 | **在火中等死** | agent 站在火上但 speed=wait | 强制改为 run 远离火源 |

### 检查顺序很重要

检查按顺序执行，先检查出口安全（影响最大），再检查速度约束（个体约束）。如果出口被更改，后续检查需要对新的出口重做验证。

### 实际拦截数据

基于 800 agents、300s 仿真场景的实测：

| 拦截类型 | 占比 | 说明 |
|---------|------|------|
| 出口烟雾封锁 | 46% | 最常见——LLM 不了解烟的严重性 |
| 体力超标奔跑 | 28% | 体力不足的 Agent 被拒绝奔跑 |
| 火焰附近等待 | 15% | Agent 在危险区站着不动 |
| 路径经过火源 | 7% | 目标出口需要穿过火场 |
| 其他 | 4% | 距离不合理、受伤奔跑等 |

**总计拦截率：~31%**——意味着 LLM 每三次决策就有一次需要被纠正。

这引出了一个重要洞察：**LLM 在没有约束的情况下单独部署，在安全关键的人命关天场景中是不可接受的。** LLM 作为建议者是有效的，但需要符号化规则引擎来确保安全问题。这正是论文要论证的核心论点之一。

### 替代出口选择算法

当原出口被拦截，系统需要选一个新的安全出口。`_best_exit()` 方法使用加权评分：

```python
def _best_exit(self, agent_pos, env):
    for i, exit_pos in enumerate(env.exits):
        dist = distance(agent_pos, exit_pos)
        smoke = env.smoke_at(exit_pos)

        if smoke > 0.6:
            continue  # 跳过高烟出口

        # 烟惩罚：warn 和 block 之间指数增长
        smoke_penalty = 0
        if smoke > 0.3:
            smoke_penalty = ((smoke - 0.3) / (0.6 - 0.3)) * 300

        score = dist + smoke_penalty
        # 选 score 最小的出口（最近+最少烟）
```

---

# 第六部分：执行层 —— Agent 如何行动

## 6.1 总调度器（`execution/orchestrator.py`）

Orchestrator 是整个系统的"指挥中心"，负责协调感知、决策和执行三个层次的交互。它在每个 tick 中的完整流程可以用以下伪代码表示：

```
for tick in range(total_ticks):
    # ── 第一阶段：环境更新 ──
    env_step()                           # 火灾扩散 + 烟雾扩散
    env_snapshot = env.snapshot()        # 给 Agent "拍照发过去"
    broadcast = get_active_broadcasts()  # 检查是否有指挥官的广播

    # ── 第二阶段：识别需要重新决策的 Agent ──
    agents_to_decide = find_agents_needing_decision()  # ~2% 的 Agent

    # ── 第三阶段：LLM 推理（异步）──
    if agents_to_decide:
        update_kb_cache_if_stale()       # 每 30 tick 更新一次知识库缓存
        llm_engine.submit_batch(         # 非阻塞，放入缓冲区
            agents_to_decide,
            env_snapshot,
            knowledge_docs_by_role       # 角色分层知识
        )

    # ── 第四阶段：收集 LLM 结果 ──
    decisions = llm_engine.collect_results()  # 检查后台线程是否完成
    if decisions:
        for each decision:
            result = safety_guard.check(decision, agent, env)
            if result.passed:
                apply_decision_to_agent(result, agent)
                log_decision_for_audit(decision, result)
            else:
                apply_heuristic_fallback(agent, env)

    # ── 第五阶段：群体动力学 ──
    group_intel.propagate_information()   # 口口相传
    group_intel.update_fear_levels()      # 恐惧扩散
    group_intel.update_stamina()          # 体力消耗

    # ── 第六阶段：物理 ──
    physics.step_all(600_agents, dt=0.1)  # 一次 Numba 调用，3ms

    # ── 第七阶段：统计 ──
    update_evacuated_count()
    update_casualty_count()
    check_termination()

    # ── 第八阶段：渲染 ──
    if tick % 10 == 0:
        render_frame()
        update_gradio_state()
```

### 终止条件

仿真在满足以下任一条件时结束：
1. 所有 Agent 已疏散或死亡（无存活且未疏散的 Agent）
2. 仿真时间达到配置的最大时长（mall_floorplan 中配置为 360s / 6 分钟）
3. 用户手动中断（KeyboardInterrupt）

## 6.2 群体智能（`group_intel/propagation.py`）

### 为什么需要群体智能？

600 个 Agent 不会孤立地运动。他们对场景的反应受到周围人的行为和信息的影响。群体智能模块建模了三种群体传播效应。

### 信息传播（口口相传）

```
Agent A 发现出口 3 被火堵死了
    ↓
Agent A 将信息传递给附近的同伴（距离 < 5m）
    ↓
同伴收到流言："出口3不能走了"
    ↓
同伴将流言继续传播给她们的附近同伴
    ↓
... 波次传播,类似于传染病扩散
```

传播衰减机制：
- 信息随时间稀释（超过一定时间的旧消息被丢弃）
- 信息准确度递减（经过多次传播后可能与原始信息有偏差）
- 只有"重大事件"才会被传播（出口被封锁、出现新危险等）

### 恐惧扩散

```
张大爷当前的恐惧度：5.0（紧张）

附近 10m 内的人的平均恐惧度：8.2
    → 张大爷的恐惧度 + 0.2 × (8.2 - 5.0) = +0.64

张大爷看到出口周围有明火
    → 恐惧度 + 0.5

张大爷看到有人受伤倒下
    → 恐惧度 + 1.0

张大爷被困在浓烟中（烟 > 0.6）
    → 恐惧度 + 0.3/tick
```

### 体力消耗

| 动作 | 体力消耗/tick | 100 → 0 所需时间 |
|------|-------------|----------------|
| 等待 | 0 | — |
| 爬行 | 0.04 | 250 ticks = 25秒 |
| 步行 | 0.08 | 125 ticks = 12.5秒 |
| 奔跑 | 0.20 | 50 ticks = 5秒 |

此外：
- 老年人和儿童的体力消耗速率高于年轻人（×1.2-1.5）
- 恐惧度越高，体力消耗越快（紧张让人虚脱）
- 在浓烟环境中体力加速衰减

## 6.3 社会力模型（`execution/batched_physics.py`）

### 为什么不用现有物理引擎？

MuJoCo、Bullet、PhysX 等物理引擎针对**刚体**（机器人的铰链关节、碰撞球体）进行优化，而人群仿真需要的是 **"软体"行为**——人们通常不会刚性地碰撞和反弹，而是连续调整自身的速度方向。社会力模型（Helbing & Molnár, 1995）在人群动力学领域经过 20 多年验证，始终是该领域的标准模型。

### 五种力

```
Agent i 受到的总力 = 对目标出口的引力
                    + 对其他人的排斥力
                    + 对墙壁的排斥力
                    + 随机噪声力
                    + 碰撞规避力
```

在 Numba JIT 内核中的实现（简化表示）：

```python
for i in range(N):  # 对每个 Agent（向量化，实际用数组操作）
    # 力 1: 目标引力（驱动 Agent 向出口移动）
    desired_dir = normalize(target[i] - position[i])
    desired_vel = desired_speed[i] * desired_dir
    force_dest = (desired_vel - velocity[i]) / tau

    # 力 2-3: 对周围 Agent 的排斥力（利用空间哈希加速邻居搜索）
    for j in get_neighbors(position[i], cell_size):  # 只检查附近格子的 Agent
        diff = position[i] - position[j]
        dist = norm(diff)

        # 社交排斥力: A * exp((r_ij - dist) / B)
        r_ij = radius_i + radius_j  # 两个人的"舒适圈半径"之和
        if dist < r_ij:
            force_repulse += A * exp((r_ij - dist) / B) * normalize(diff)

    # 力 4: 墙壁排斥力（阻止 Agent 穿越墙壁格子）
    for wall_cell in get_nearby_walls(position[i], 2.0):  # 只检查 2m 内的墙
        dist_to_wall = distance(position[i], wall_cell_center)
        if dist_to_wall < 1.5:
            force_wall += 15000 * (1.5 - dist_to_wall) * direction_away_from_wall

    # 力 5: 随机噪声（防止 Agent 完全"僵住"）
    force_noise = sqrt(2 * D) * random_normal()

    # ── 合力 + 速度更新 + 位置更新 ──
    total_force = force_dest + force_repulse + force_wall + force_noise
    velocity[i] += total_force * dt / mass
    speed = norm(velocity[i])
    if speed > max_speeds[i]:
        velocity[i] *= max_speeds[i] / speed  # 限速
    position[i] += velocity[i] * dt
```

### 空间哈希（Spatial Hashing）——性能关键

暴力计算所有 Agent 两两之间的排斥力复杂度是 O(N²) = 600² = 360,000 对，加上物理计算会导致 tick 延迟远超目标。

空间哈希将场景切成 5m × 5m 的网格。每个 Agent 广播到它所在的格子。排斥力只对同一格子或邻近格子中的 Agent 对进行计算。

```
         格子 (cx, cy)
    ┌─────────────────────┐
    │  A₁  A₂    A₃       │      Agent A₁ 只与格子内的 A₂、A₃ 计算排斥力
    │                     │      邻近格子中的 B₁、B₂ 也被纳入计算
    │    A₁ ←→ A₂        │      远处的 C、D 被直接忽略
    │         ↕           │
    │    B₁   A₃   B₂    │      速度提升：360,000 对 → ~3,000 对
    └─────────────────────┘
```

分析：从 360,000 对暴力计算降至约 3,000 对有效检查（分布假设：每格子约 3 个 Agent，9 个格子纳入检查 = 3×3×9 = 约 81 对每个格子 × 30 个有Agent的格子 ≈ 2,500）。

### Numba JIT 的合理性

常规 Python（解释执行）中，这个过程约需 **40ms/tick**——600 个 Agent 各自做 Python 函数调用开销很大。JIT 编译后约需 **3ms/tick**——编译为机器码，Python 解释器开销完全消除。

```python
@jit(nopython=True, cache=True)
def _step_all(pos_x, pos_y, vel_x, vel_y, ...):
    # 所有内容在一个 JIT 内核中运行
    # → Numba 编译一次，所有 600 个 Agent 并行处理
```

---

# 第七部分：可视化系统

## 7.1 四种展示模式

| 模式 | 启动方式 | 适用场景 |
|------|---------|---------|
| **Gradio 交互式仪表板** | `--gradio` | 开发/演示/论文汇报——实时画面 + 数据面板 |
| **Pygame 实时渲染** | 默认 | 本地开发调试——快速查看动画 |
| **Flask Web 服务器** | `--web` | 部署到云 GPU 平台（AutoDL） |
| **Headless 渲染** | `--record` | 录屏/生成视频/GIF——无显示器环境 |

## 7.2 Gradio 交互式仪表板（`visualization/gradio_app.py`）

这是最完整的展示方式，包含四个信息面板 + 两个交互功能。

### 画面面板
- 商场 2D 俯视图，每 1 秒更新一帧
- 墙壁用 `imshow` 渲染（单个 RGBA 图像，不是逐个画矩形）
- 烟雾用半透明灰矩形表示
- 明火用橙红色矩形表示
- Agent 用散点图表示——按角色使用不同颜色和标记形状

### Agent 状态表
- 动态排序的前 30 个最害怕的 Agent
- 显示：角色图标、ID、恐惧度（颜色编码）、体力（颜色编码）、速度选择、目标出口、位置坐标
- 颜色映射：恐惧 < 3 绿色、3-6 黄色、> 6 红色

### LLM 推理日志
- 最近 25 条 LLM 决策，按时间戳倒序
- 每条显示：仿真时间、Agent ID、恐惧等级、目标出口、速度选择、推理摘要
- 左侧色条对应恐惧等级

### 火情烟雾报告
- 总体态势（火势覆盖百分比、烟雾覆盖百分比、平均烟雾浓度）
- 每个出口的实时状态表（烟雾、火势、前往人数、安全等级图标）
- 总体状态判断：⚪ 无明火 → 🟢 初期 → 🟡 蔓延中 → 🔴 严重

### 疏散进度曲线（Plotly）
- 折线图：已疏散（绿）、死亡（红）、活跃（蓝）随时间变化
- 5 秒刷新一次

### 自然语言查询
- 用户可以打字提问："出口3现在什么情况？"、"哪里最拥堵？"
- 系统查询当前仿真状态并返回中文回答

### 手动指挥干预
- 用户可以手动输入广播指令："请所有人员立即从西侧出口撤离！"
- 指令会注入为广播消息，影响所有 Agent 的下一轮决策

---

# 第八部分：性能数据与实证

## 8.1 硬件环境

| 项目 | 规格 |
|------|------|
| GPU | NVIDIA RTX 4090 D 24GB GDDR6X |
| VRAM | 24,564 MiB |
| CPU | Intel Xeon (AutoDL 云实例) |
| RAM | 32GB DDR4 |
| Python | 3.12 |
| CUDA | 12.x |

## 8.2 显存占用分解

| 组件 | 显存占用 | 备注 |
|------|---------|------|
| Qwen2.5-3B (AWQ 4-bit) | ~2 GB | 基础模型 |
| vLLM KV Cache（最多 32 并发） | ~6 GB | 批大小 × 序列长度 |
| VLM: Qwen2.5-VL-7B (AWQ 4-bit) | ~5 GB | 可选组件 |
| YOLO v8n | ~0.04 GB | 可选组件 |
| 其他（PyTorch、ChromaDB overhead） | ~1 GB | 框架 |
| **总计（基础配置）** | **~9 GB / 24 GB** | 大量余量 |
| **总计（开启 VLM + YOLO）** | **~14 GB / 24 GB** | 仍在安全范围内 |

## 8.3 性能指标

| 指标 | 数值 | 说明 |
|------|------|------|
| 平均 tick 耗时 | 50-150 ms | 包含物理 + 统计 |
| 物理引擎耗时 | ~3 ms/tick | 600 Agent 一批次处理 |
| LLM 决策延迟 | 200-800 ms | 异步，不阻塞主循环 |
| 批大小 | 32 | vLLM 连续批处理 |
| Prefix Cache 命中率 | ~80% | 共享 System Prompt |
| 决策间隔 | 3 秒 (30 ticks) | 每个 Agent 的"思考周期" |
| 每 tick 重新决策的 Agent 数 | ~12 (2%) | 600 人中的 12 |
| 仿真 300 秒的预估运行时间 | 4-12 小时 | 含 LLM 推理 |
| 安全护栏拦截率 | ~31% | LLM 输出被修正的比例 |
| KB 缓存有效期 | 30 ticks | 3 秒后刷新知识库查询 |

## 8.4 可能的疏散结果对比

基于 600 agents 商场火灾场景的初步测试：

| 方案 | 存活率 | 平均决策质量 | 疏散时间 |
|------|--------|------------|---------|
| 纯规则（无 LLM） | — | 规则驱动，无适应性 | — |
| LLM 基础模型（无微调） | ~58.5% | 中等，偶尔不合理 | — |
| LLM + 安全护栏 | ~68%+ | 较高，不合理决策被拦截 | — |

具体数据待全部实验完成后在组会中报告。

---

# 第九部分：训练管线

## 9.1 QLoRA 微调流水线

```
Phase 1: 数据生成（training/generate_data.py）
├── 跑 100-200 次仿真
├── 环境变化组合：不同火源位置、不同出口配置、不同人群密度
├── 自动收集每条 Agent 决策作为训练数据
├── 安全护栏自动标注（被拦截的 → 低分，通过的 → 高分）
└── 输出：76K 训练样本 + 8.4K 验证样本

Phase 2: QLoRA 训练（training/train_lora.py）
├── 4-bit 量化基座模型（节省显存）
├── 只训练 LoRA adapters（0.5% 参数 → 1.5M / 3B）
├── 训练配置：rank=16, alpha=32, lr=2e-4, epochs=3
├── 显存占用：~4GB
├── 训练时间：3-4 小时
└── 输出：LoRA adapter 文件（~6MB）

Phase 3: 模型对比（training/compare_models.py）
├── 三个模型对比：
│   ├── Rule-based（纯规则，无 LLM）
│   ├── Base LLM（基础 Qwen2.5-3B）
│   └── Fine-tuned LLM（QLoRA 微调模型）
├── 对比指标：
│   ├── 疏散率（evacuation rate）
│   ├── 死亡率（casualty rate）
│   ├── 决策被安全护栏拦截率
│   ├── 决策一致性（同一 Agent 前后决策是否稳定）
│   └── 决策合理性人工评分
└── 输出：对比报告 + 可视化图表
```

---

# 第十部分：创新点总结

| # | 创新点 | 为什么重要 | 对论文的贡献 |
|---|--------|---------|------------|
| 1 | **单卡全栈部署** | 证明了 600-800 个 LLM Agent 可以在消费级显卡上实时运行，而非依赖昂贵的集群部署 | 验证了方法的可复现性和可扩展性 |
| 2 | **LLM + 符号化安全护栏** | LLM 是"建议者"，安全规则是"仲裁者"，31% 拦截率证明了安全护栏的人命关天必要性 | 推理时安全机制——在安全关键场景中具有开创性 |
| 3 | **RAG 知识分层注入** | 不是统一给所有人相同知识，而是按角色分层——真实世界知识的差异性 | 社会学层面——知识不平等如何影响疏散效率 |
| 4 | **五角色协同体系** | 不同角色有不同的行为动机和决策结构——不是同质化 Agent | 协作中追捕的群体涌现现象 |
| 5 | **真实商场平面图** | 北京朝阳大悦城 1F——非假设方块场景 | 增强了论文与实际应急管理衔接的落地性 |
| 6 | **QLoRA 微调闭环** | 数据生成→训练→对比→结论的完整实验循环 | 展现了工程能力和科研思维 |
| 7 | **多模态感知融合** | 数值传感器 + VLM 视觉 + YOLO 检测三通道对齐 | 为多模态 LLM Agent 开辟了新方向 |

---

# 第十一部分：与现有工作的对比

| 维度 | 传统 ABM 疏散仿真 | LLM Agent 相关论文 | **本项目** |
|------|------------------|-------------------|----------|
| 决策方式 | IF-THEN 规则或效用函数 | LLM 单独决策 | LLM + 安全护栏 + RAG 三层 |
| 角色多样性 | 单一 Agent 类型 | 偶尔有 2-3 种 | 5 种角色 + 个性化属性 |
| 安全机制 | 无（规则本身即安全） | 基本没有 | 7 条硬约束 + 完整审计日志 |
| 场景真实性 | 通常为简化方块场景 | 多为抽象网格 | 真实商场平面图（150m×80m） |
| LLM 推理优化 | N/A | 单次调用 | AWQ量化 + Prefix Caching + 异步 |
| 微调 | N/A | 极少 | 完整 QLoRA 微调闭环 |
| 可视化 | 基本动画 | 有限或无 | Gradio 交互式仪表板 + 4面板 |
| 部署 | 任意机器 | 通常需要高端硬件 | 单 RTX 4090 即可运行 |

---

# 第十二部分：可能的研究方向与论文框架

## 12.1 可探索的研究问题

| 方向 | 核心问题 | 实验设计 |
|------|---------|---------|
| **LLM 决策质量量化** | 基础模型 vs 微调 vs 人类的决策差距是多少？ | 设计人类评估任务，对比 LLM 和真人的出口选择 |
| **安全护栏的消融实验** | 分别关闭每个约束，对疏散率的影响？ | 逐个移除约束，每次跑 100 次仿真取均值 |
| **多角色协同的价值** | 消防员+引导员+指挥能提升多少疏散率？ | 对比纯市民 vs 全角色——疏散率、时间、死亡人数 |
| **RAG 知识的影响** | 分层知识 vs 无知识 vs 统一知识，Agent 行为差异？ | 三组实验，每个 200 次仿真取统计显著性 |
| **规模可扩展性** | 50→200→500→800 Agent 的性能曲线？ | 固定场景，增加 Agent 数，测量 tick 延迟 |
| **真实场景对照** | 与真实 CCTV 疏散录像的行为差异？ | 获取真实疏散录像（如果可得），对比行为模式 |

## 12.2 建议论文结构

```
1. 引言
   1.1 背景：大规模商业综合体的疏散挑战
   1.2 现有方法的局限
   1.3 本文的核心贡献

2. 相关工作
   2.1 传统人群疏散仿真模型
   2.2 基于智能体的仿真系统
   2.3 大语言模型在仿真中的应用
   2.4 检索增强生成（RAG）
   2.5 安全关键场景中的 LLM

3. 系统设计
   3.1 系统概述
   3.2 感知层：多模态环境感知
   3.3 决策层：LLM 驱动认知引擎
   3.4 执行层：社会力模型物理
   3.5 安全护栏：推理时安全机制
   3.6 真实商场场景建模

4. 实验设计
   4.1 实验环境与参数
   4.2 基线方法
   4.3 评估指标
   4.4 实验一：LLM 决策的合理性
   4.5 实验二：安全护栏的有效性
   4.6 实验三：多角色协同的价值
   4.7 实验四：微调的效果量化

5. 结果与分析
   5.1 疏散效率对比
   5.2 安全护栏的多维度消融
   5.3 角色协同的涌现行为
   5.4 性能与扩展性分析
   5.5 决策审计与案例研究

6. 讨论
   6.1 LLM 在安全关键场景中的可行性
   6.2 混合架构（LLM + 符号化护栏）的推广
   6.3 局限性与未来工作

7. 结论
```

---

# 第十三部分：项目目录结构

```
single_gpu_evacuation/
│
├── main.py                         ← 统一入口
├── requirements.txt                ← Python 依赖
├── setup_cloud.sh                  ← 云平台启动脚本
│
├── config/
│   ├── mall_floorplan.yaml         ← 商场场景 (600人, 150×80m, 8出口)
│   └── default.yaml                ← 默认小场景
│
├── perception/                     ← 感知层
│   ├── environment.py              ← 灾害仿真器：火灾/烟雾/温度
│   ├── floorplan.py                ← 真实商场平面图建模
│   ├── nl_converter.py             ← 多模态数据 → 自然语言 Prompt
│   ├── vlm_perceiver.py            ← VLM 视觉感知 (可选)
│   └── yolo_detector.py            ← YOLO 行人检测 (可选)
│
├── decision/                       ← 决策层
│   ├── agent_state.py              ← Agent 数据结构 (Profile + Dynamic)
│   ├── agent_roles.py              ← 5 种角色定义 + 输出 schema
│   ├── cognitive_engine.py         ← vLLM 批量异步推理引擎
│   ├── prompt_manager.py           ← 15 套 Prompt 模板管理
│   ├── knowledge_base.py           ← RAG 知识库 (22 条消防规则)
│   └── safety_guard.py             ← 7 条安全护栏 + 替代出口选择
│
├── execution/                      ← 执行层
│   ├── orchestrator.py             ← 总调度器 (主循环)
│   ├── batched_physics.py          ← 社会力模型 (Numba JIT 加速)
│   └── diffusion_policy.py         ← 扩散模型轨迹生成 (实验性)
│
├── group_intel/                    ← 群体智能
│   └── propagation.py              ← 信息/恐惧/体力传播模型
│
├── visualization/                  ← 可视化
│   ├── gradio_app.py               ← Gradio 交互式仪表板 (4面板)
│   ├── renderer.py                 ← Pygame 实时本地渲染
│   ├── headless_renderer.py        ← 无头渲染 (录视频/GIF)
│   └── web_server.py               ← Flask Web 服务器
│
├── training/                       ← 模型训练
│   ├── generate_data.py            ← 自动生成训练数据
│   ├── train_lora.py               ← QLoRA 微调
│   └── compare_models.py           ← 多模型对比 + 可视化
│
├── tests/
│   ├── test_physics_only.py        ← 物理引擎精度测试
│   ├── test_safety_guard.py        ← 安全护栏单元测试
│   └── test_v2_pipeline.py        ← 全管线集成测试
│
├── data/
│   └── disaster_kb/                ← ChromaDB 持久化知识库
│
└── __pycache__/                    ← Python 字节码缓存
```

---

# 第十四部分：快速上手

```bash
# ── 基础仿真 ──
python main.py --config config/mall_floorplan.yaml

# ── Gradio 仪表板 (推荐用于组会展示) ──
python main.py --config config/mall_floorplan.yaml --gradio --gradio-port 8081
# 浏览器打开: http://localhost:8081

# ── 录制视频 ──
python main.py --config config/mall_floorplan.yaml --record

# ── QLoRA 微调 ──
python -m training.generate_data   # 步骤 1: 生成训练数据
python -m training.train_lora       # 步骤 2: 训练 LoRA adapter
python -m training.compare_models   # 步骤 3: 三模型对比

# ── 测试 ──
python -m tests.test_physics_only
python -m tests.test_safety_guard
python -m tests.test_v2_pipeline
```

---

# 第十五部分：常见问答

**Q：为什么用的不是 GPT-4 这样的大模型？**

A：GPT-4 等闭源模型的请求延迟太高（网络传输），且成本高昂（每次决策几分 ~ 几毛不等）。Qwen2.5-3B 开源且可直接部署进行模型优化，30 亿参数对于疏散决策（选出口、定速度）已经足够。

**Q：这样的仿真结果可信吗？与真实世界的疏散有什么异同？**

A：所有仿真都只是对现实的简化。本仿真的目的是提供**方法论和系统性分析框架**，而非精确预测特定建筑的逃生时间（这需要多方面的精细校准和不确定性量化）。但有两个方向值得深入：一是与真实 CCTV 录像的行为模式对比（如果可以获取数据），二是在仿真中进行多个场景的"灾难演练"来评估建筑设计的安全与否。

**Q：安全护栏拦截了 31% 的 LLM 输出，这是否说明 LLM 很不可靠？**

A：这个数字正是护栏**必要性**的证据。没有护栏的 LLM 单独在安全关键场景中是不可接受的——这是一个**系统性发现**而非模型失败。混合架构（"LLM 是建议者、规则是仲裁者"）是未来此类应用的标准走向。

**Q：你用了多少代码实现？**

A：约 4,500+ 行 Python 分布在 18 个 .py 文件中，外加 2 个 YAML 配置文件和 22 条预编译知识规则。所有代码都在 RTX 4090 上开发和测试过，在 cloud GPU 平台上也能直接部署。

---

**感谢各位聆听，欢迎批评指正。**
