# 问题分析与解决方案（v5 重训版）

**项目**：LLM→IRL→RL 三级联级人群疏散仿真（single_gpu_evacuation）

**日期**：2026-08-15

**目标**：解释 v4 权重为何失败，以及为修复这些问题所做的三处代码改动、验证结果与云端重训步骤。

---

## 一、当前状态摘要

- v3（旧训练环境）在真实 LLM 仿真中与 Heuristic 无显著差异；
- 烟雾/火路过滤（方案 A）没有带来收益；
- v4（极限环境重训第一版）在**自己的训练环境里**都比无调度差：
  - 独立评估：v4 疏散率 78.5%，无调度 94.0%，随机 MLP 92.1%；
  - 训练历史显示训练“健康”但奖励与目标错位；
- 根因已定位：**奖励函数优化的是推荐动作的即时特征，不是疏散结果；且训练与部署的出口屏蔽规则不一致**。
- 解决方案：三处代码修复（训练 rollout 屏蔽、奖励惩罚扩展、结果导向奖励），验证通过，待云端重训 v5。

---

## 二、问题清单与根因

### P1（致命）：v4 在自己训练环境里输给无调度

在“和训练完全同款”的极限模拟器上（200 agents、双火源 0.22、火源随机、300s、4 轮）：

| 条件 | 疏散率 | 伤亡率 |
|---|---|---|
| 无调度（纯启发式） | 94.0% | 6.0% |
| 启发式建议 + IRL | 91.5% | 8.5% |
| **v4 策略** | **78.5%** | **21.5%** |
| 随机 MLP | 92.1% | 7.9% |

结论：v4 不能用于正式对比；这不是采样噪声，是策略本身学坏了。

### P2：训练历史显示“训练成功但目标错位”

从 `rl_policy_v4.json.history.json`（500 轮）提取：

| 指标 | 前 100 轮 | 后 100 轮 | 判断 |
|---|---|---|---|
| MeanR | 0.303 | 0.346 | 上升 ✅ |
| PolicyLoss | −0.0021 | −0.0038 | O(1e-3)，无爆炸 ✅ |
| ValueLoss | 23.3 | 10.0 | 收敛 ✅ |
| 训练内疏散率 | 68%~73% | 84%~92% | 有提升，但**从未超过无调度 94%** ⚠️ |
| 训练内伤亡率 | 32% | ~12% | 下降 ✅ |

关键矛盾：**MeanR 涨了，疏散率却没有跟着涨到基线以上**——奖励在优化别的东西。

### P3：奖励与真实目标脱节

`zone_reward_features` 用 IRL 权重给“出口推荐向量”即时打分，其中：

- `conformity`（从众）：推荐“多数人已选的出口”就能拿高分，即使该出口很远或穿过火场；
- `safety` 只看出口当前烟雾，不预测路径风险；
- 奖励里没有疏散结果信号（存活率、到达进度）。

所以策略可以“刷分”而不救人。

### P4：训练与部署不一致（关键机制性 bug）

- **推理时**（`inject_rl_preferences`）：出口烟雾 > 0.6 **或路径过火** 都会被屏蔽；
- **训练时**（`blocked_exit_penalty`）：奖励只惩罚“出口有烟”，**不惩罚路径过火**；
- 结果：策略在训练里学会推荐“路径过火”的出口（拿其它奖励分），部署时这些推荐全被过滤掉——**训练学的和部署用的不是同一个动作空间**。

### P5（历史工程问题，已处理）

| 问题 | 状态 |
|---|---|
| 旧 500 轮训练未收敛、checkpoint ≈ 随机 | 已判定无效，弃用 |
| `rl_scheduler.initialize()` 文件缺失也打印 “Loaded” | 已修复日志 |
| 训练权重缺失时静默回退启发式 | 已用 `rl_policy_loaded` 暴露 |
| LLM 无固定 seed，实验噪声大 | 已加 `fixed_seed` |
| 安全修改/拦截只有总数，无法分规则 | 已加 7 类规则计数 |
| 云容器重启丢代码（/output 不持久） | 已改为 /hyperai/home 持久目录 |
| 新旧文件不同步导致 TypeError | 已建立上传+验证流程 |
| 南出口 E6 坐标落在墙格上 | 已修正 floorplan |
| 硬配置“双火源”第二源只是障碍物 | 已实现真多火源 |

---

## 三、解决方案（三处代码改动）

### S1：训练 rollout 与推理使用同一套出口屏蔽

**文件**：`execution/rl_scheduler.py`

新增向量化函数：

```python
_vectorized_blocked_exits(positions, env_snapshot,
                          smoke_block_threshold=0.6, samples=16)
```

- 对每个 agent、每个出口，沿直线采样 17 个点（与护栏 `_check_fire_proximity` 同语义）；
- 返回 `(出口烟雾封锁, agent×出口路径过火)` 两个布尔矩阵；
- 全向量化，200 agents × 6 出口开销 <1ms/step。

`FastTrainingSimulator.step()` 在收到 `zone_actions` 时计算该矩阵，agent 重新选出口时**跳过被封锁出口**（全部封锁则选烟雾最低的兜底）——和部署侧“护栏+建议过滤”行为对齐。

### S2：奖励惩罚覆盖“出口有烟 或 路径过火”

**文件**：`execution/features.py`、`execution/rl_scheduler.py`

- `blocked_exit_weight()` 新增 `blocked_exits` 参数，支持传入预计算封锁集合；
- `_compute_zone_reward()` 用 `_vectorized_blocked_exits` 对 zone 内 agent 采样（前 40 个）计算封锁出口，凡推荐权重落在这些出口上就扣 `blocked_exit_penalty`（配置 1.0）；
- 这样训练惩罚与部署过滤规则完全一致。

### S3：结果导向奖励项

**文件**：`execution/rl_scheduler.py`

新增 `_outcome_shaping()`，每步给 zone 奖励叠加：

```text
outcome = 0.4 × 靠近出口程度
        + 0.3 × (1 − 平均所在位置烟雾)
        + 0.3 × 平均体力
```

权重由配置 `rl_scheduling.outcome_reward_weight` 控制（极限配置设为 0.3）。

目的：让 RL 优化的不只是“推荐动作好不好看”，还包括“人群是否真的在安全地接近出口”。

### S4a：优化信号增强（v4 权重分析后的补充修复）

对 `rl_policy_v4` 权重的定量分析显示：策略头 W3_a 与随机初始化的最大偏差仅 0.01~0.024（初始化尺度 0.01），500 轮后基本没动；而价值头 W3_v 移动了 ~0.5。**v4 不是"学坏了"而是"没学到"——actor 优化信号被三重衰减抹平**（lr=3e-4、按样本数归一化、max_norm=1.0 裁剪，单步有效更新 ~1e-6）。

**文件**：`execution/rl_scheduler.py`

| 改动 | 原值 | 新值 |
|---|---|---|
| `LEARNING_RATE` | 3e-4 | **3e-3**（1e-3 冒烟实测 delta 仅 ~0.003/轮，仍不够） |
| `GRAD_MAX_NORM`（策略梯度裁剪） | 1.0 | 5.0 |
| `CRITIC_EPOCHS`（每轮 critic SGD 次数） | 1 | 4（ValueLoss 停在 ~10，价值函数欠拟合拖累 advantage 质量） |
| advantage 标准化 | 减均值除 std | **只除 std 不减均值**（零均值化会抹掉本就微弱的方向性团队信号） |
| 停滞监控 | 无 | `policy_head_delta` 连续 100 轮 <1e-3 时打印 WARNING |
| `_outcome_shaping` 的 closeness | 对任意最近出口 | 对**推荐出口的 softmax 加权目标点**（给动作本身分配信用） |
| `_outcome_shaping` 的体力项 | 平均体力（移动会掉体力→激励原地不动） | 体力>30 的 agent 占比（存活指示） |

本地冒烟（10 轮、200 agents、极限环境）：策略头 10 轮净位移 0.019（≈初始化尺度 2 倍），PolicyLoss O(1) 无爆炸，无 NaN。脚本：`results/_smoke_v5_train.py`。

### S4：配套配置

`config/mall_floorplan_rl_extreme.yaml` 的 `rl_scheduling` 段：

```yaml
smoke_block_threshold: 0.6
fire_path_block: true
blocked_exit_penalty: 1.0
origin_jitter: 15.0
outcome_reward_weight: 0.3
```

---

## 四、验证结果

### 单元测试（全部通过）

| 测试 | 验证内容 |
|---|---|
| `test_vectorized_blocked_exits` | 烟雾/火路屏蔽矩阵正确 |
| `test_blocked_exit_weight_with_precomputed_set` | 预计算封锁集合的惩罚权重正确 |
| `test_training_rollout_masks_fire_path_exit` | agent 不会选择路径过火的出口 |
| `test_outcome_shaping_orders_close_agents_higher` | 更接近出口的 agent 得到更高 shaping |
| 原有 12 个 RL 测试 | 无回归 |
| 原有 smoke filter / safety counter / multi-origin 测试 | 无回归 |

### 短训练冒烟（本地 10 轮，200 agents）

```text
Ep  0/10 | MeanR: +0.394 | Evac: 70.5%
Ep  5/10 | MeanR: +0.415 | Evac: 77.5%
Ep  9/10 | MeanR: +0.438 | Evac: 56.0%
```

训练管线正常（短轮次疏散率波动属正常噪声；完整 500 轮才看趋势）。

---

## 五、云端执行步骤

### 1. 上传 5 个文件（/hyperai/home 和 /output 都要）

```text
execution/rl_scheduler.py
execution/features.py
execution/orchestrator.py
experiments/full_pipeline.py
config/mall_floorplan_rl_extreme.yaml
```

### 2. 验证上传

```bash
grep -c "outcome_reward_weight" execution/rl_scheduler.py config/mall_floorplan_rl_extreme.yaml
grep -c "_vectorized_blocked_exits" execution/rl_scheduler.py
```

两边都应有输出（>0）。

### 3. 训练 v5

```bash
python main.py --config config/mall_floorplan_rl_extreme.yaml \
  --train-rl --train-rl-episodes 500 \
  --irl-weights data/irl_weights.json \
  --rl-output data/rl_policy_v5.json
```

预计 1~3 小时。

### 4. 指向 v5 并重跑 5 种子对比

```bash
sed -i 's#data/rl_policy_v[0-9.]*\.json#data/rl_policy_v5.json#' config/mall_floorplan_rl_extreme.yaml

nohup python -m experiments.compare_rl_heuristic \
  --rl-config config/mall_floorplan_rl_extreme.yaml \
  --heuristic-config config/mall_floorplan_extreme.yaml \
  --agents 200 --duration 360 \
  --seeds 42 43 44 45 46 \
  --output results/compare_rl_extreme_v5_5seeds \
  > results/compare_rl_extreme_v5_console.log 2>&1 &
```

预计 4~6 小时。

---

## 六、验收标准

### 训练阶段

- PolicyLoss 保持在 O(1e-3)~O(1) 量级，无爆炸；
- 训练内疏散率应逐步接近 90%+（基线无调度 94%）；
- **核心指标（v4 教训）**：策略头定量偏离初始化——保存权重与同 seed 随机初始化对比，`max|W3_a − init| ≥ 0.05`（即初始化尺度 0.01 的 5 倍以上）；部署推理时偏好幅度（tanh 后）应 ≥0.2 才有实际影响力；
- `policy_head_delta` 连续 ≥1e-3（若触发停滞 WARNING 说明优化信号仍不足，先调超参再谈奖励设计）；
- 注意：W3_v/价值头移动不能代替上面的策略头检查（v4 就是价值头动了、策略头没动）。

### 对比阶段

- `rl loaded = Y`（全部 5 轮）；
- 疏散率配对差异翻正：RL ≥ Heuristic（哪怕 +1pp 且 p<0.05）；
- `fire_path_swap` / `exit_smoke_swap` 相对 v4 明显下降；
- 若 v5 训练结束 Evac 仍 <90%，或对比仍打平/更差，则下一步按“奖励再设计”（直接以疏散结果做稀疏奖励 + 更强 shaping）处理，而不是继续加过滤。

---

## 七、关键结论一句话

**v4 的问题有两层：一是"奖励和部署规则对不上"**——训练时允许推荐火路出口、奖励又偏向从众刷分，导致策略在部署时被过滤后失去价值；**二是"优化信号太弱"**——权重分析证明 v4 的策略头从未离开随机初始化尺度，actor 实际上什么都没学到。v5 通过"训练=部署的屏蔽规则 + 结果导向奖励"对齐目标，再通过"lr/裁剪/critic 轮数/advantage 归一化"四处超参修复让策略头真正移动。
