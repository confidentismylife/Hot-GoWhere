# 项目问题总结报告

**项目**：基于 LLM→IRL→RL 三级联级的人群疏散多智能体仿真（single_gpu_evacuation）

**报告日期**：2026-08-13

**范围**：综合 2026-08-06 至今的代码审查、权重审查、多轮实验（5 种子 RL vs Heuristic、烟雾过滤对比）与云端运行过程中发现的问题。

---

## 一、结论摘要（TL;DR）

| # | 问题 | 严重度 | 状态 |
|---|------|:---:|------|
| 1 | 旧 500 轮 RL 训练未收敛，checkpoint 约等于随机初始化 | 🔴 | 已确认无效，需重训 |
| 2 | `rl_scheduler.initialize()` 日志误导：文件不存在也打印 “Loaded pretrained weights” | 🔴 | ✅ 已修复 |
| 3 | 训练权重缺失时静默回退为启发式建议，结果被误当成“RL 效果” | 🔴 | ✅ 已暴露（rl_policy_loaded），需运行时确认 |
| 4 | 真实 LLM 仿真中 RL（v3 权重）与 Heuristic 无显著差异，5 种子 p≈0.55 | 🔴 | 未解决 |
| 5 | RL 建议显著增加安全护栏修改次数（p≈0.013），增益被护栏抵消 | 🟠 | 未解决 |
| 6 | 烟雾出口过滤（方案 A）未改善结果：修改次数未降、疏散率未升 | 🟠 | 假设 A 不成立 |
| 7 | LLM 采样无固定 seed，实验噪声大（同条件两次运行差 ~8 次修改） | 🟠 | 待修复 |
| 8 | 安全修改/拦截只有总数，无法区分规则类型，诊断能力不足 | 🟠 | 待修复 |
| 9 | 云端缺少 chromadb，RAG 实际走关键词兜底，论文 RAG 声明未验证 | 🟠 | 待处理 |
| 10 | 论文/README 的性能与结果数字来自合成管线，与真实 LLM 仿真差异巨大 | 🔴 | 需统一口径 |

---

## 二、问题清单

### 2.1 RL 训练与策略

**1. 旧 500 轮训练未收敛（详见 `500轮训练结果审查报告.md`）**

- PolicyLoss 从第 200 轮开始爆炸，最高达 **1,203,346**；ValueLoss 全程 1000–1400 不收敛。
- 日志中“疏散率 58.7% → 38.2%”是 `FastTrainingSimulator.reset()` 未重置体力/恐惧/目标出口造成的**状态泄漏假象**。
- 保存权重与同 seed 随机初始化几乎一致（W1 最大偏差 ≤0.06、W3_a ≤0.05），策略层等效未训练。
- 4 个 zone 两两相关系数 ≥0.9999，完全同质。
- **结论**：该 checkpoint 不能用于论文中任何“Ours / 训练后 RL”结果。

**2. v3 权重（`rl_policy_v3.json`）本身是真的训练过的，但迁移失效**

- 结构正确：4 zones、31 维观测（6 出口）、128 维隐藏、双头 MLP，无 NaN。
- 与随机初始化对比：W1 最大偏差 0.12–0.17、W2 0.08–0.11、W3_v 0.48–0.53，价值头显著移动；策略头 W3_a 偏差仅 0.006–0.012（≈初始化尺度）。
- 行为层面与随机 MLP 明显不同（偏好强度 0.37 vs 0.08；argmax 一致率仅 25%），四个 zone 各学出主导出口，且避开了训练环境火源附近的出口。
- 在**训练环境**（快速模拟器）A/B 中表现最好：疏散率 99.3%、伤亡 0.7%，优于无调度（96.2%/3.8%）、启发式建议（89.3%/0.5%）、随机 MLP（90.8%/2.0%）。
- 但迁移到**真实 LLM 仿真**后收益消失——典型的训练/部署分布偏移（训练：600 人、dt=1、火源固定在出口 1/4、规则 agent；部署：82+ 人、dt=0.1、火源 [170,100]、LLM 决策 + 安全护栏）。

**3. 训练环境与部署环境不一致（根因）**

- `FastTrainingSimulator` 的火源位置、蔓延速率、dt、agent 数、决策机制与真实配置均不同。
- 重训时应至少对齐：火源位置/随机化、dt、agent 数、烟雾动态、出口集合，并在奖励中惩罚“推荐被阻塞出口”。

### 2.2 代码与日志缺陷

**4. `rl_scheduler.initialize()` 误导性日志（已修复）**

- 原逻辑：只要 `pretrained_path` 非空就打印 `[RLScheduler] Loaded pretrained weights from ...`，**即使文件不存在、已回退为启发式**。
- 后果：之前用 `grep "RLScheduler" results/rl_82.log` 验证“RL 已加载”是**无效证据**。
- 修复后：文件不存在会打印 `WARNING: pretrained weights NOT FOUND ... using heuristic baselines`；`rl_policy_loaded` 记录真实加载状态。

**5. 权重缺失时静默回退（已暴露，待运行确认）**

- 配置文件指向 `data/rl_policy_v3.json`，但该文件不在仓库（`data/` 被 `.gitignore` 忽略），本地复现/验证困难。
- 对比脚本现在会在预检中警告，并在每一行结果标注 `rl loaded = Y/N`。

**6. 安全护栏只有聚合计数，无法按规则诊断（待修复）**

- `safety_blocks` / `safety_modifications` 是总数；修改由 7 类规则触发（出口烟雾换出口、路径过火换出口、体力降速、受伤降速、着火强制跑、距离异常换出口、危险位置禁等待）。
- 出口烟雾只是其中一条，速度类规则与 RL 建议无关却占大头——导致“过滤出口建议”对总修改数几乎无影响。
- **建议**：在 `SafetyGuard` 增加每类规则计数器，报告中分列。

**7. LLM 采样无固定 seed（待修复）**

- vLLM `SamplingParams` 未设置 `seed`，同一配置两次运行之间随机性很大：
  - heuristic 组两次运行：修改次数差 **8.4 次**（p=0.06），疏散率差 0.58pp（p=0.50）。
  - 所有“过滤前后”差异（+4.2 次修改、−0.76pp）都淹没在该噪声内。
- **建议**：`SamplingParams(seed=...)` 按 run 固定，压低实验噪声。

**8. 其他小问题**

- `main.py` 原本没有 `--seed`（已修复，现支持 CLI 覆盖）。
- orchestrator 注释称 command agents 不参与社会力物理，但 `batched_physics.step_all` 实际处理所有存活 agent，注释与实现不符。
- `execution/social_force.py`、`execution/spatial_grid.py` 源文件缺失，仅残留 `__pycache__` 与 `missing_files.tar.gz`（内含 pyc），存在陈旧产物混淆风险。
- `LLMCognitiveEngine.shutdown()` 通过 psutil 强杀含 “vllm/enginecore” 的进程，多进程场景可能误杀其他任务。

### 2.3 实验设计与统计

**9. 单次运行不能下结论**

- 早期单次对比（RL 59.8% vs Heuristic 61.8%）曾被解读为“RL 略差”，但多种子后证明该差距是噪声。

**10. 5 种子正式对比：RL ≈ Heuristic（无过滤）**

| 指标 | Heuristic | RL | 配对差 (H−R) | t | p |
|---|---|---|---|---|---|
| 总体疏散率 | 53.5% | 52.9% | +0.56 pp | 0.64 | 0.55 |
| 伤亡率 | 24.1% | 24.7% | −0.58 pp | −1.22 | 0.29 |
| 普通人疏散率 | 48.6% | 48.1% | +0.44 pp | 0.75 | 0.50 |
| 安全修改 | 47.4 | 72.6 | −25.2 | −4.28 | **0.013** |
| 安全拦截 | 37.8 | 29.2 | +8.6 | 3.15 | 0.035 |
| 平均 tick | 33.8ms | 45.0ms | −11.2 | −158 | <0.001 |

- 疏散率/伤亡率无显著差异；唯一稳定的差异是 **RL 显著多触发安全修改**。
- RL 每 tick 稳定多耗 ~11ms（观测构建中 `_fire_distance` 对整张网格多次 `np.where`），与“<1ms”的文档声明不符。

**11. 烟雾过滤（方案 A）验证失败**

- 实现：`inject_rl_preferences()` 在生成建议前，将烟雾 > 0.6 的出口硬屏蔽（偏好置 −1.0），文本标注“[封锁] 浓烟封锁，请勿前往”。
- 5 种子结果：
  - 过滤后 RL：疏散率 52.2%、修改 76.8；不过滤 RL：疏散率 52.9%、修改 72.6。
  - 差异：−0.76pp（p=0.29）、+4.2 次修改（p=0.40），均在噪声内。
  - 过滤后 RL 的修改次数仍显著高于 heuristic（p=0.011）。
- **结论**：建议-护栏冲突不是 RL 无效的主因（或“修改总数”不是正确观测口径），假设 A 按当前指标不成立。

**12. 统计口径问题（已部分修复）**

- `--agents 82` 只是普通 agent 数，最终还会加入 1 全局指挥官 + N 区域指挥官 + 消防员 + 引导员 + 家庭组成员；此前疏散率分母包含全部 agent。
- 已新增 civilian / command 分组统计；建议正式结果以“普通人疏散率”为主指标。

### 2.4 环境与工程问题

**13. 云端缺少 chromadb**

- 日志反复出现 `[KB] Semantic search unavailable ... No module named 'chromadb'`，知识库实际使用关键词兜底。
- RL/Heuristic 两组受影响相同，A/B 依然公平；但论文若声称“RAG 语义检索有效”，需 `pip install chromadb sentence-transformers` 后重新验证。

**14. 云任务管理风险**

- 曾出现：8 种子任务被 Stopped 但 GPU 显存仍被占用，导致新任务 vLLM 加载失败（Exit 1）。
- 建议统一 `nohup ... &`（或 `disown -h`）后台运行；先 `nvidia-smi`/`ps aux | grep vllm` 确认无残留。

**15. 本地开发环境不完整**

- 本地 Python 3.13、numpy/numba/torch(CPU)，无 vLLM、无 pytest、无 chromadb；LLM 实验只能在云端跑，单元测试只能部分执行。

### 2.5 文档与论文一致性

**16. 论文/README 数字与真实仿真口径不一致**

- 合成管线报告（`data/experiments/summary.md`）中 Ours 疏散率 91.7%、伤亡 2.7%；真实 LLM 仿真（82 agents、mall、360s）仅约 52–53% 疏散、24% 伤亡。
- 两者场景、规模、物理/伤害标定不同，**不能混用**；论文需明确标注实验模式（合成管线 / 真实 LLM），并核实高伤亡率是否来自伤害概率标定。

**17. 文档声明未全部实测**

- README 中的 120ms/tick、11GB 显存、LoRA 15MB adapter、QLoRA 76K 样本等数字多为设计/计划值；真实运行中 100 agents + LLM 平均 tick 约 33–48ms。
- “12 万条轨迹”与“2 万条轨迹”在不同文档中出现，需要统一。
- P-MAPPO 命名已统一为“分区独立 PPO（Zone-PPO）”。

**18. 论文正文仍是草稿**

- `paper/main.tex` 含大量 `\todo{}`（摘要、实验数字、消融、结论），参考文献含未来日期条目，投稿前需逐条核对。

---

## 三、已实施的修复/改进

| 文件 | 改动 |
|---|---|
| `main.py` | 新增 `--seed` 参数 |
| `execution/orchestrator.py` | 角色分组统计（civilian/command）、`rl_policy_loaded`、avg/max tick 记录；支持注入共享 LLM 引擎且不随单次运行关闭 |
| `execution/rl_scheduler.py` | 修复误导性加载日志；RL 建议烟雾出口过滤（`smoke_block_threshold`，默认 0.6） |
| `decision/cognitive_engine.py` | `initialize()` 幂等保护；新增 `reset_stats()` 支持引擎复用 |
| `experiments/compare_rl_heuristic.py` | 多种子 RL vs Heuristic 自动对比；默认一次加载 vLLM 跑完全部种子；自动保存 JSON/MD 报告；逐行记录 `rl loaded` |
| `config/mall_floorplan_rl_v3.yaml` | 新增 `smoke_block_threshold: 0.6` |
| `tests/test_rl_smoke_filter.py` | 烟雾过滤单元测试 |

---

## 四、遗留问题与下一步建议（按优先级）

1. **给安全护栏加按规则计数器 + vLLM 固定 seed**（改动小，约 1 小时）：
   - 分别统计：出口烟雾切换、火路切换、体力降速、受伤降速、着火强制跑、距离异常、危险位置等待；
   - `SamplingParams(seed=...)` 按 run 固定；
   - 用同一 5 种子重跑，才能把“过滤到底有没有用”和“修改到底来自哪条规则”定位清楚。
2. **根据新数据决定**：
   - 若出口烟雾类修改确实下降但总修改不变 → 速度类规则主导，建议层过滤不是关键，转方案 B；
   - 若各项仍无差异 → 接受“RL 建议在真实 LLM 场景无增益”，论文如实报告。
3. **方案 B（重训贴近部署）**：让 `FastTrainingSimulator` 与真实场景对齐（火源随机化、dt、agent 数、烟雾模型），奖励惩罚推荐被阻塞出口，重训 v3。
4. **环境补全**：云端安装 `chromadb sentence-transformers` 后复测 RAG；将 `data/rl_policy_v3.json` 纳入版本管理或提供下载地址保证可复现。
5. **论文口径**：明确区分合成管线与真实 LLM 仿真两套数字；核对 README/论文中的性能声明。

---

## 五、关键数据附录

### 5.1 真实 LLM 仿真（mall 200×120m，82 普通 agent，360s，5 seeds）

| 条件 | 疏散率 | 伤亡率 | 普通疏散率 | 安全修改 | 安全拦截 | 平均 tick |
|---|---|---|---|---|---|---|
| Heuristic（无过滤轮） | 53.5% | 24.1% | 48.6% | 47.4 | 37.8 | 33.8ms |
| RL v3（无过滤） | 52.9% | 24.7% | 48.1% | 72.6 | 29.2 | 45.0ms |
| Heuristic（过滤轮） | 52.9% | 24.3% | 48.1% | 55.8 | 36.8 | 35.4ms |
| RL v3 + 烟雾过滤 | 52.2% | 24.5% | 47.7% | 76.8 | 30.6 | 47.2ms |

### 5.2 快速模拟器策略 A/B（600 agents，360 steps，8 episodes）

| 策略 | 疏散率 | 伤亡率 |
|---|---|---|
| 无调度（纯启发式） | 96.2% | 3.8% |
| 启发式建议 + IRL 权重 | 89.3% | 0.5% |
| 训练后 v3 | 99.3% | 0.7% |
| 随机初始化 MLP | 90.8% | 2.0% |

### 5.3 配对显著性（5 seeds）

| 比较 | 指标 | 差值 | p |
|---|---|---|---|
| Heuristic − RL（无过滤） | 疏散率 | +0.56 pp | 0.55 |
| Heuristic − RL（无过滤） | 安全修改 | −25.2 | 0.013 |
| RL 过滤 − RL 不过滤 | 疏散率 | −0.76 pp | 0.29 |
| RL 过滤 − RL 不过滤 | 安全修改 | +4.2 | 0.40 |
| Heuristic 两次运行（噪声基线） | 安全修改 | 8.4 | 0.06 |

---

*本文档由代码审查 + 权重分析 + 两轮 5 种子实验 + 云端运行日志综合整理；数据均可在仓库与 `C:/Users/摩天轮大盗/Downloads/compare_rl_heuristic_5seeds*.md` 中追溯。*
